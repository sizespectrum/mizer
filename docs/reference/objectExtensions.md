# Get the extensions stored in a mizer object

Get the extensions stored in a mizer object

## Usage

``` r
objectExtensions(object)
```

## Arguments

- object:

  A `MizerParams` or `MizerSim` object.

## Value

A named character vector of extensions, or an empty character vector if
the object carries no extensions.
