# Remove a dynamic marker class, including a stale cached definition

Remove a dynamic marker class, including a stale cached definition

## Usage

``` r
removeDynamicMarkerClass(class)
```

## Arguments

- class:

  Character string — the S4 class name to remove.

## Value

Invisibly, whether `methods` reported removing the class.
