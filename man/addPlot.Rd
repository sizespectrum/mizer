% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArraySpeciesBySize-class.R,
%   R/ArrayTimeBySpecies-class.R, R/ArrayTimeBySpeciesBySize-class.R,
%   R/ArrayResourceBySize-class.R
\name{addPlot}
\alias{addPlot}
\alias{addPlot.ArraySpeciesBySize}
\alias{addPlot.ArrayTimeBySpecies}
\alias{addPlot.ArrayTimeBySpeciesBySize}
\alias{addPlot.ArrayResourceBySize}
\alias{addPlot.ArrayTimeByResourceBySize}
\title{Add lines to an existing plot}
\usage{
addPlot(
  plot,
  x,
  species = NULL,
  total = FALSE,
  background = TRUE,
  colour = NULL,
  linetype = "dashed",
  linewidth = 0.8,
  alpha = 1,
  ...
)
}
\arguments{
\item{plot}{A ggplot2 object to which the new values should be added.}

\item{x}{An object containing the values to add. Can be an
\code{ArraySpeciesBySize}, \code{ArrayTimeBySpecies}, \code{ArrayTimeBySpeciesBySize},
\code{ArrayResourceBySize} or \code{ArrayTimeByResourceBySize} object.}

\item{species}{Character vector of species to include. \code{NULL} (default) means
all species. A resource array holds a single spectrum, so this argument is
not used by the resource methods, which warn if it is set.}

\item{total}{A boolean value that determines whether the total is plotted
as well. The total is the total of everything the array holds, every
species and every size, whatever is drawn. Default is \code{FALSE}. Not used by
the resource methods, which warn if it is set.}

\item{background}{A boolean value that determines whether background species
are included. Ignored if the model does not contain background species.
Default is \code{TRUE}. Not used by the resource methods, which warn if it is
set.}

\item{colour}{Optional fixed colour for the added lines. If \code{NULL}, the
species colours from the existing plot are used. Because a resource array
is a single line whose "Resource" level may be missing from the existing
plot's colour scale, the resource methods instead default to the fixed
resource colour from \code{getColours()}.}

\item{linetype}{Optional fixed line type for the added lines. If \code{NULL}, the
species line types from the existing plot are used.}

\item{linewidth}{Width of the added lines.}

\item{alpha}{Transparency of the added lines.}

\item{...}{Further arguments used by only some of the methods:

\strong{For the \code{ArraySpeciesBySize}, \code{ArrayTimeBySpeciesBySize},
\code{ArrayResourceBySize} and \code{ArrayTimeByResourceBySize} methods:}
\describe{
\item{\code{wlim}}{A numeric vector of length two providing lower and upper
limits for the weight (x) axis. Use \code{NA} to refer to the existing
minimum or maximum.}
}

\strong{For the \code{ArraySpeciesBySize} and \code{ArrayTimeBySpeciesBySize} methods:}
\describe{
\item{\code{all.sizes}}{If \code{FALSE} (default), values outside a species' size
range (\code{w_min} to \code{w_max}) are removed.}
\item{\code{llim}}{A numeric vector of length two providing lower and upper
limits for the length (x) axis when \code{size_axis = "l"}. Use \code{NA} to
refer to the existing minimum or maximum.}
\item{\code{size_axis}}{Whether to plot size as weight (\code{"w"}, default) or
length (\code{"l"}), using the allometric weight-length relationship of
each species, or of the resource, see \code{\link[=resource_params]{resource_params()}}.}
\item{\code{per_log_size}}{For an array that holds a density, whether to plot
it per logarithmic size (\code{TRUE}) rather than per size (\code{FALSE}). The
default, \code{NULL}, plots the density as it stands. Unlike \code{size_axis}
this needs no weight-length relationship, so it is available for the
resource classes too. An error for an array that does not hold a
density.}
}

\strong{For \code{ArrayTimeBySpecies} methods:}
\describe{
\item{\code{tlim}}{A numeric vector of length two providing lower and upper
limits for the time axis, e.g. \code{c(1980, 2000)}. Use \code{NA} to apply no
limit at that end. Default is \code{c(NA, NA)}.}
\item{\code{ylim}}{A numeric vector of length two providing lower and upper
limits for the value (y) axis.}
}

\strong{For the \code{ArrayTimeBySpeciesBySize} and \code{ArrayTimeByResourceBySize}
methods:}
\describe{
\item{\code{time}}{The time to display. Default (\code{NULL}) is the final time
step.}
}}
}
\value{
A ggplot2 object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
\code{addPlot()} adds another set of values to an existing ggplot, for example to
compare the same rate before and after a model change. There are methods for
all the mizer array classes. Each checks whether the existing plot uses a
compatible x variable, and warns if the y variable or y-axis units appear to
differ.
}
\examples{
\donttest{
p <- plot(getEncounter(NS_params), species = "Cod")
addPlot(p, getEncounter(NS_params), species = "Cod")

pr <- plot(getResourceMort(NS_params))
addPlot(pr, getResourceMort(NS_params))
}
}
\seealso{
Other plotting functions:
\code{\link[=animate]{animate()}},
\code{\link{plot}},
\code{\link[=plot2]{plot2()}},
\code{\link[=plotBiomass]{plotBiomass()}},
\code{\link[=plotCDF]{plotCDF()}},
\code{\link[=plotCDF2]{plotCDF2()}},
\code{\link[=plotDiet]{plotDiet()}},
\code{\link[=plotFMort]{plotFMort()}},
\code{\link[=plotFeedingLevel]{plotFeedingLevel()}},
\code{\link[=plotGrowthCurves]{plotGrowthCurves()}},
\code{\link{plotMizerParams}},
\code{\link{plotMizerSim}},
\code{\link[=plotPredMort]{plotPredMort()}},
\code{\link[=plotRelative]{plotRelative()}},
\code{\link[=plotSpectra]{plotSpectra()}},
\code{\link[=plotSpectra2]{plotSpectra2()}},
\code{\link[=plotSpectraRelative]{plotSpectraRelative()}},
\code{\link[=plotYield]{plotYield()}},
\code{\link[=plotYieldGear]{plotYieldGear()}},
\code{\link[=plotYieldVsF]{plotYieldVsF()}},
\code{\link{plotting_functions}}
}
\concept{plotting functions}
