% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sizeIntegral.R
\name{size_dim_labels}
\alias{size_dim_labels}
\title{Identify the dimensions of an array over the size grid}
\usage{
size_dim_labels(x, arg, no_sp, no_w)
}
\arguments{
\item{x}{The array to label.}

\item{arg}{The name of the argument holding \code{x}, used in error messages.}

\item{no_sp}{The number of species in the model.}

\item{no_w}{The number of size bins in the model.}
}
\value{
A character vector with one label for each dimension of \code{x}.
}
\description{
Internal helper for \code{\link[=sizeIntegral]{sizeIntegral()}}. Returns a label for each dimension of
\code{x}. The last dimension must run over the size grid and is labelled \code{"w"}.
Other dimensions are labelled from the names of their dimnames, if they have
any, with \code{"species"} and \code{"size"} normalised to mizer's \code{"sp"} and \code{"w"}.
An unnamed second-to-last dimension is labelled \code{"sp"} if its extent is the
number of species. Any remaining unnamed dimension gets a unique label of its
own, so that it is carried through to the result rather than matched against
a dimension of the abundance.
}
\details{
A scalar has no dimensions and gets no labels.
}
\concept{helper}
\keyword{internal}
