% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_setters.R
\name{scanEffort}
\alias{scanEffort}
\alias{scanFishingMortality}
\alias{scanSpeciesParam}
\title{Setters for scanning a model}
\usage{
scanEffort(gear = NULL)

scanFishingMortality(species, gear = NULL)

scanSpeciesParam(species, parameter)
}
\arguments{
\item{gear}{For \code{scanEffort()}, the name of the gear whose effort is scanned,
or NULL (default) to scan the effort of every gear together. For
\code{scanFishingMortality()}, the name of the gear whose fishing mortality on
the target species is scanned. Only needed when several gears catch the
species; if NULL, the fishing mortality from all of them is replaced.}

\item{species}{The name of the target species.}

\item{parameter}{The name of the species parameter to scan.}
}
\value{
A function of \verb{(params, value)} returning a \code{MizerParams} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
These functions build the \code{set_func} that \code{\link[=scanModel]{scanModel()}} uses to apply each
scan value to the model. Each returns a function of \verb{(params, value)} that
returns a modified \code{MizerParams} object, carrying attributes that let
\code{\link[=scanModel]{scanModel()}} label the axis and mark reference lines without being told.
}
\details{
You are not restricted to these. Any function of \verb{(params, value)} returning
a \code{MizerParams} will do, as long as it is \strong{idempotent}: with
\code{continuation = TRUE} it is applied to the object it returned at the previous
scan value, so applying it twice must give the same thing as applying it
once. Setting a value is idempotent; appending something is not, which is why
\code{\link[=scanFishingMortality]{scanFishingMortality()}} checks whether its gear is already there.

\describe{
\item{\code{scanEffort()}}{Scans the fishing effort. With \code{gear = NULL} the same
effort is applied to every gear, which is what a bifurcation diagram over
fishing effort needs.}
\item{\code{scanFishingMortality()}}{Scans the fishing mortality on one species
while leaving the fishing on every other species alone. It does this by
adding a temporary gear that catches only the target species with
catchability 1, so that its effort \emph{is} the fishing mortality, and
switching off the catchability of the gears it replaces. If several gears
catch the species you can name the one whose mortality is to be varied,
and the others go on fishing unchanged. The added gear is given a name
the model is not already using, so a model that happens to have a gear
called \code{"scan"} is not disturbed.}
\item{\code{scanSpeciesParam()}}{Scans any species parameter. It assigns to
\code{\link[=species_params]{species_params()}}, so the value is recorded as a given one and the
change propagates through to the rates that depend on it. The parameter
has to be one the model already has; add the column first if it is not.}
}
}
\examples{
\donttest{
# The fishing mortality on Cod alone, leaving the other species alone
plot(scanModel(NS_params, scan_values = seq(0, 1.2, 0.3),
               set_func = scanFishingMortality("Cod"),
               value_func = getYield, species = "Cod"))
}
}
\seealso{
\code{\link[=scanModel]{scanModel()}}

Other scan functions:
\code{\link[=MizerScan]{MizerScan()}},
\code{\link[=plot.MizerScan]{plot.MizerScan()}},
\code{\link[=plotYieldVsF]{plotYieldVsF()}},
\code{\link[=scanModel]{scanModel()}}
}
\concept{scan}
\concept{scan functions}
