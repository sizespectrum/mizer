% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info_signals.R
\name{signal_frozen}
\alias{signal_frozen}
\title{Signal that a change the user made cannot take effect}
\usage{
signal_frozen(var, message)
}
\arguments{
\item{var}{A string naming the quantity the report is about.}

\item{message}{The message to give the user.}
}
\value{
\code{NULL} invisibly. Called for its side effect of signalling.
}
\description{
A rate array that has been set by hand is protected by a comment, see the
"Setting or changing rates" section in \code{\link[=setParams]{setParams()}}. Mizer then no longer
calculates it from the species parameters, so a change to one of the species
parameters that feeds it has no effect on the model. This function raises
the condition that tells the user so.
}
\details{
The condition is raised at severity \code{"warning"}, see \code{\link[=signal_info]{signal_info()}}, so
that it survives the \code{suppressMessages()} that \code{\link[=species_params<-]{species_params<-()}} runs
over its recalculation. It also carries the class \code{info_about_frozen} for
code that wants to catch this kind of report in particular.

Only signal this when the user has actually asked for something that is not
happening. The mere fact that a frozen array differs from what the formula
would give is not enough: mizer freezes arrays itself when it builds the
trait-based and community models, and those arrays differ from the formula
for the lifetime of the model. See \code{\link[=signal_frozen_changes]{signal_frozen_changes()}}, which decides
this from the species parameters the user changed.
}
\concept{helper}
