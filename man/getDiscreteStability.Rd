% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steadyNewton.R
\name{getDiscreteStability}
\alias{getDiscreteStability}
\title{Analyse the stability of mizer's numerical time step}
\usage{
getDiscreteStability(
  params,
  reproduction = c("fixed", "dynamic"),
  effort = params@initial_effort,
  h = 1e-04,
  dt = 1
)
}
\arguments{
\item{params}{A \linkS4class{MizerParams} object whose \code{initial_n} holds the
steady state to analyse. Typically the output of \code{\link[=steadyNewton]{steadyNewton()}}.}

\item{reproduction}{Whether the reproduction rate is held fixed (\code{"fixed"},
default) or run dynamically (\code{"dynamic"}) during the evaluation.
Must match the choice used when the steady state was computed.}

\item{effort}{The fishing effort to use. By default the initial effort
stored in \code{params}.}

\item{h}{Relative step size for centred finite differences. Default \code{1e-4}.
The result should not depend on this choice. If it does, the dynamics are
not smooth at the state being analysed — see the section below.}

\item{dt}{The time step size of the one-step map. Default \code{1}.}
}
\value{
A named list with the following components:
\describe{
\item{\code{discrete_eigenvalues}}{Complex vector of the eigenvalues
\eqn{\mu_i} of the one-step map, sorted by decreasing modulus.}
\item{\code{spectral_radius}}{\eqn{\max_i|\mu_i|}. Less than 1 means the
numerical scheme is stable at this \code{dt}.}
\item{\code{stable}}{Logical: \code{TRUE} when \code{spectral_radius < 1}.}
\item{\code{dt}}{The step size the map was evaluated at.}
\item{\code{n_active}}{Dimension of the Jacobian.}
\item{\code{leading_eigenvectors}}{The eigenvectors of the two
largest-modulus eigenvalues, in the same shape as for
\code{\link[=getStability]{getStability()}}.}
\item{\code{params}}{The validated \code{params} object the analysis was made at.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Computes the eigenvalues \eqn{\mu_i} of the linearised one-step-ahead map at
the steady state stored in \code{params@initial_n}, for a given step size \code{dt}.
These describe how mizer's numerical scheme, rather than the model, behaves
near the steady state: the map does not amplify perturbations when the
spectral radius \eqn{\max_i|\mu_i|} is less than 1.
}
\details{
This is the numerical counterpart of \code{\link[=getStability]{getStability()}}, which analyses the
model itself and involves no time step at all. Use \code{getStability()} to ask
whether the steady state of the \emph{model} is stable, and this function to ask
what mizer's solver does at a particular \code{dt}. The two can disagree, and that
disagreement is the point: the implicit transport solve damps oscillations
artificially, so a physically unstable steady state can have a spectral
radius below 1 at a large \code{dt}, and the simulation then sits at a state the
model does not actually hold.
\subsection{The map that is linearised}{

One step is what \code{\link[=project]{project()}} takes with \code{method = "euler"}: the rates are
evaluated at the state at the start of the step, and the resulting transport
problem is solved implicitly,
\deqn{A(N^t, n_{pp}^t)\,N^{t+1} = S(N^t, n_{pp}^t),}
with the same \code{project_n_loop()} C++ Thomas solver and the same spatial
scheme (\code{\link[=second_order_w]{second_order_w()}}) as the regular dynamics.

Because the rates are evaluated at the \emph{input} state, the step is not fully
implicit, and the discrete eigenvalues therefore cannot be converted into
continuous-time eigenvalues by any exact algebraic relation. That conversion
is what \code{\link[=getStability]{getStability()}} avoids by differentiating the rates of change
themselves.

The resource is stepped with backward Euler,
\deqn{n_{pp}^{t+1} = \frac{n_{pp}^t + dt\,r_{pp}c_{pp}}%
                          {1 + dt\,(r_{pp} + \mu_R^t)},}
rather than with the exact semichemostat solution that
\code{\link[=resource_semichemostat]{resource_semichemostat()}} uses in \code{project()}. The two agree to first order
in \code{dt} for the slow modes that decide stability, while backward Euler keeps
the fast resource modes away from a discrete eigenvalue of exactly zero.
}
}
\section{Requires smooth dynamics}{

The finite-difference Jacobian is only meaningful if the rates of change are
differentiable at \eqn{N^*}. A custom rate function registered with
\code{\link[=setRateFunction]{setRateFunction()}} that jumps as a function of the abundances breaks this in
two ways. If the state sits on the switching threshold, some perturbations
straddle it and pick up the jump, and the reported eigenvalues then vary
wildly with \code{h}. If the state is near but not on the threshold, no
perturbation crosses it, and the function silently returns the stability of
the single branch the state happens to lie on — which can read as \code{stable}
for a model whose simulations never settle.

Re-running with a different \code{h} is the cheapest check: if the answer moves,
do not trust it. See \href{https://sizespectrum.org/mizer/articles/discontinuous_rates.html}{Discontinuous rate functions}.
}

\seealso{
\code{\link[=getStability]{getStability()}}, \code{\link[=steadyNewton]{steadyNewton()}}
}
