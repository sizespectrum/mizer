% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayResourceBySize-class.R
\name{ArrayResourceBySize_plot_data}
\alias{ArrayResourceBySize_plot_data}
\title{The complete plotting data of a resource-by-size array}
\usage{
ArrayResourceBySize_plot_data(
  x,
  wlim = c(NA, NA),
  llim = c(NA, NA),
  size_axis = "w",
  per_log_size = NULL
)
}
\arguments{
\item{x}{An \code{ArrayResourceBySize} object.}

\item{wlim}{Numeric vector of length two giving the weight limits.}

\item{llim}{Numeric vector of length two giving the length limits, applied
only on a length axis.}

\item{size_axis}{Either \code{"w"} (weight) or \code{"l"} (length).}

\item{per_log_size}{Whether to express a density per logarithmic size.}
}
\value{
A data frame with the size coordinate in its first column, the values
in its second, and \code{Species} and \code{Legend} columns.
}
\description{
The resource analogue of \code{\link[=ArraySpeciesBySize_plot_data]{ArraySpeciesBySize_plot_data()}}: the weight limits,
the conversion of the values and of the size coordinate onto the requested
axis, and the length limits, all done with the array's own \code{params}. A
resource array holds a single spectrum, so there is no selection, no
background and no total to form.
}
\keyword{internal}
