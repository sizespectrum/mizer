% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registerExtensions.R
\name{extensionClassEnvironment}
\alias{extensionClassEnvironment}
\title{Get the environment holding mizer's dynamic marker classes}
\usage{
extensionClassEnvironment(create = TRUE)
}
\arguments{
\item{create}{Logical. If \code{TRUE}, create and attach the environment when it
does not yet exist. If \code{FALSE}, return \code{NULL} instead.}
}
\value{
The attached environment, or \code{NULL} when it does not exist and
\code{create = FALSE}.
}
\description{
The environment is attached to the search path, lazily, the first time a
dispatching extension needs a dynamic marker class. A session using base
mizer, or only metadata-only extensions, therefore never acquires the extra
search path entry.
}
\details{
The search path is where this metadata has to live. \code{.GlobalEnv} does not
work, because \verb{R CMD check} empties it with \code{cleanEx()} between examples and
users clear it themselves (#587). An environment that mizer merely holds
privately does not work either, however well it survives: a params object
saved before mizer started creating marker classes dynamically carries a
class attribute whose package slot names the extension package, and
\code{methods} then resolves the class with an inheriting lookup that starts in
that package's namespace and runs on through \code{.GlobalEnv} and the rest of
the search path. It never passes through mizer's own namespace, so only the
search path catches every case.

\code{cleanEx()} also detaches whatever an example added to the search path, so
the environment lasts a whole check run only when it was attached while the
extension package was being loaded, before \verb{R CMD check} recorded the search
path. That is what happens when an extension registers itself from
\code{.onLoad}. Losing it the other way is not fatal: \code{\link[=markerClassPresent]{markerClassPresent()}} then
reports the classes as gone and the next registration rebuilds them.

The environment is deliberately left mutable, because rebuilding an
extension chain requires removing and re-parenting its classes. The S4
package identity of the classes stays \code{.GlobalEnv}, which is what marks them
as mutable dynamic classes rather than static classes owned by an extension
namespace.
}
\keyword{internal}
