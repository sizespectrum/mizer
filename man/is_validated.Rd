% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MizerParams-class.R
\name{is_validated}
\alias{is_validated}
\alias{record_validated}
\alias{clear_validated_params}
\title{Keep track of which MizerParams objects have been fully validated}
\usage{
is_validated(key)

record_validated(key, max_size = validated_params_max)

clear_validated_params()
}
\arguments{
\item{key}{A fingerprint as returned by \code{\link[=validation_key]{validation_key()}}.}

\item{max_size}{The number of fingerprints to keep. When the record has grown
to this size it is emptied.}
}
\value{
\code{is_validated()} returns TRUE if the fingerprint has been recorded.

\code{record_validated()} returns \code{NULL}, invisibly.

\code{clear_validated_params()} returns \code{NULL}, invisibly. The next
validation of any object then takes the full path again.
}
\description{
Because the fingerprint returned by \code{\link[=validation_key]{validation_key()}} determines the outcome
of \code{\link[=repair_params]{repair_params()}} and of the structural validity checks, an object whose
fingerprint has been recorded by a previous call to \code{\link[=validParams]{validParams()}} needs
neither. The record lasts for the R session only.
}
\keyword{internal}
