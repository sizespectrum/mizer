% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{resolve_spectrum_power}
\alias{resolve_spectrum_power}
\title{Resolve the power of weight multiplying a spectrum}
\usage{
resolve_spectrum_power(power = NULL, biomass = NULL, per_log_size = NULL)
}
\arguments{
\item{power}{The power of weight multiplying the number density, or \code{NULL}.}

\item{biomass}{Whether to plot a biomass density rather than a number
density, or \code{NULL}. The default is \code{TRUE}.}

\item{per_log_size}{Whether to plot a density with respect to logarithmic
size rather than with respect to size, or \code{NULL}. The default is \code{FALSE}.}
}
\value{
A list with entries \code{power}, \code{biomass} and \code{per_log_size}.
}
\description{
The quantity plotted by \code{\link[=plotSpectra]{plotSpectra()}} is the number density multiplied by
\code{w^power}. That power is the sum of two independent choices: whether the
quantity is a biomass density (a factor of \code{w}) or a number density, and
whether it is a density with respect to logarithmic size (another factor of
\code{w}) or with respect to size. The two choices are what determine the y-axis
label and the Jacobian used when converting to a length axis, and they are
not recoverable from \code{power} alone: \code{power = 1} is both the biomass density
with respect to weight and the number density with respect to logarithmic
weight.
}
\details{
Users therefore express the choice with the \code{biomass} and \code{per_log_size}
flags. The \code{power} argument remains available, both for backwards
compatibility and as an escape hatch for powers that are not the sum of two
flags. Each argument is \code{NULL} when it was not supplied by the user.
}
\keyword{internal}
