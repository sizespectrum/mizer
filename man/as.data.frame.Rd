% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic_methods.R
\name{as.data.frame}
\alias{as.data.frame}
\alias{as.data.frame.ArraySpeciesBySize}
\alias{as.data.frame.ArrayTimeBySpecies}
\alias{as.data.frame.ArrayTimeBySpeciesBySize}
\title{Convert mizer arrays to data frames}
\usage{
as.data.frame(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An \code{ArraySpeciesBySize}, \code{ArrayTimeBySpecies} or
\code{ArrayTimeBySpeciesBySize} object.}

\item{row.names}{Optional and included only for compatibility with the base
generic. \code{NULL} or a character vector giving the row names for the
data frame.}

\item{optional}{Optional and included only for compatibility with the base
generic. A logical value. If \code{TRUE}, setting row names and converting
column names (to syntactic names) is optional.}

\item{...}{Further arguments. They are currently ignored by the mizer
methods.}
}
\value{
A data frame in long format.
}
\description{
The \code{as.data.frame()} methods for mizer array classes turn matrix- and
array-like results into tidy long-form data frames, with one row per
observed combination of species, size and/or time. The numeric result is
always stored in a column called \code{value}.
}
\details{
The returned columns are:
\itemize{
\item \code{ArraySpeciesBySize}: \code{w}, \code{value}, \code{Species}.
\item \code{ArrayTimeBySpecies}: \code{time}, \code{value}, \code{Species}.
\item \code{ArrayTimeBySpeciesBySize}: \code{time}, \code{Species}, \code{w}, \code{value}.
}

If the original object has non-numeric or missing dimension names, sequential
indices are used for the \code{time} or \code{w} columns. Species names are taken from
the row, column or dimension names of the original object.
}
\examples{
\donttest{
enc <- getEncounter(NS_params)
head(as.data.frame(enc))

biomass <- getBiomass(NS_sim)
head(as.data.frame(biomass))
}
}
\seealso{
\code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=str]{str()}}, \code{\link[=plot]{plot()}}, \code{\link[=ArraySpeciesBySize]{ArraySpeciesBySize()}},
\code{\link[=ArrayTimeBySpecies]{ArrayTimeBySpecies()}}, \code{\link[=ArrayTimeBySpeciesBySize]{ArrayTimeBySpeciesBySize()}}
}
