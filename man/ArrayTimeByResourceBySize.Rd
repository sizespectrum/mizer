% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayResourceBySize-class.R
\name{ArrayTimeByResourceBySize}
\alias{ArrayTimeByResourceBySize}
\alias{is.ArrayTimeByResourceBySize}
\title{S3 class for time x resource-size arrays}
\usage{
ArrayTimeByResourceBySize(x, value_name = NULL, units = NULL, params = NULL)

is.ArrayTimeByResourceBySize(x)
}
\arguments{
\item{x}{A matrix (time x size). For \code{is.ArrayTimeByResourceBySize()}, any
object to test.}

\item{value_name}{A string giving the human-readable name for the value.}

\item{units}{A string giving the units (e.g. "1/g").}

\item{params}{A \code{MizerParams} object. Used for the resource colour and the
size grid in the \code{plot()} method.}
}
\value{
An \code{ArrayTimeByResourceBySize} object (inherits from \code{matrix} and
\code{array}).

\code{is.ArrayTimeByResourceBySize()} returns \code{TRUE} if \code{x} is an
\code{ArrayTimeByResourceBySize} object, \code{FALSE} otherwise.
}
\description{
The \code{\link[=NResource]{NResource()}} function returns a two-dimensional array (time x size)
holding the resource number density through time. The
\code{ArrayTimeByResourceBySize} class wraps this array to provide convenient
\code{print()}, \code{summary()}, \code{plot()}, and \code{as.data.frame()} methods.
}
\details{
An \code{ArrayTimeByResourceBySize} object behaves just like a regular matrix for
arithmetic operations and subsetting. It carries these lightweight
attributes:
\itemize{
\item \code{value_name} – a human-readable name for the value
(e.g. "Number density").
\item \code{units} – the units of the value (e.g. "1/g").
\item \code{params} – the \code{MizerParams} object that the value was computed from.
}
}
\examples{
\donttest{
nr <- NResource(NS_sim)
is.ArrayTimeByResourceBySize(nr)
summary(nr)
plot(nr)
}
}
\seealso{
\code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=as.data.frame]{as.data.frame()}}, \code{\link[=plot]{plot()}}
}
