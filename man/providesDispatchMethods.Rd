% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registerExtensions.R
\name{providesDispatchMethods}
\alias{providesDispatchMethods}
\title{Does an installed extension register dispatch methods for its own class?}
\usage{
providesDispatchMethods(name)
}
\arguments{
\item{name}{The extension identifier (its marker class / package name).}
}
\value{
\code{TRUE} if the loaded namespace \code{name} registers S3 methods for class
\code{name} or \code{paste0(name, "Sim")}, otherwise \code{FALSE}.
}
\description{
An extension package participates in dispatch by registering S3 methods for
mizer generics keyed on its marker class (e.g. \code{getEncounter.mizerMR}). This
checks the package namespace's own S3 method registry for any method whose
class is the extension name or its sim variant. Because S3 method
registration does not require the S4 marker class to exist, this lets mizer
recognise a dispatching extension \emph{before} creating its class, so extension
packages no longer have to define the marker class statically. Defining it
statically as \code{contains = "MizerParams"} would in fact prevent the package
from being chained with other extensions, since a sealed class cannot be
re-parented into the chain (see \code{\link[=defineExtensionClasses]{defineExtensionClasses()}}).
}
\keyword{internal}
