% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion.R
\name{getDiffusion}
\alias{getDiffusion}
\title{Get diffusion rate from predation}
\usage{
getDiffusion(object, ...)
}
\arguments{
\item{object}{A \linkS4class{MizerParams} or \linkS4class{MizerSim} object.}

\item{...}{Additional arguments that depend on the class of \code{object}.

\strong{For a \linkS4class{MizerParams} object:}
\describe{
\item{\code{n}}{A matrix of species abundances (species x size). Defaults to
the initial abundances stored in \code{object}.}
\item{\code{n_pp}}{A vector of the resource abundance by size. Defaults to the
initial resource abundance stored in \code{object}.}
\item{\code{n_other}}{A named list of the abundances of other dynamical
components. Defaults to the initial values stored in \code{object}.}
\item{\code{t}}{The time for which to do the calculation. Defaults to 0.}
}

\strong{For a \linkS4class{MizerSim} object:}
\describe{
\item{\code{time_range}}{The time range over which to return the rates. Either
a vector of values, a vector of min and max time, or a single value.
Defaults to the whole time range of the simulation.}
\item{\code{drop}}{If \code{TRUE} then any dimension of length 1 is removed from the
returned array.}
}}
}
\value{
\itemize{
\item \code{MizerParams}: An \code{ArraySpeciesBySize} object (predator species x predator
size) with the diffusion rates.
\item \code{MizerSim}: An \code{ArrayTimeBySpeciesBySize} object (time step x predator
species x predator size) with the diffusion rates at every time step.
If \code{drop = TRUE} then dimensions of length 1 will be removed.
}
}
\description{
Calculates the diffusion rate \eqn{D_i(w)} (grams^2/year) for each species.
This diffusion rate has two components:
\enumerate{
\item The diffusion due due to the variability in prey sizes. This is the
diffusion term from the jump-growth equation.
\item Any externally specified diffusion, which is added via \code{\link[=setExtDiffusion]{setExtDiffusion()}}
}
}
\details{
The diffusion due due to the variability in prey sizes
is determined by summing over all prey
species and the resource spectrum and then integrating over all prey sizes
\eqn{w_p}, weighted by predation kernel \eqn{\phi(w,w_p)}:
\deqn{
d_i(w) = (1-f_i(w))(\alpha_i(1-\psi_i(w)))^2\gamma_i(w) \int
\left( \theta_{ip} N_R(w_p) + \sum_{j} \theta_{ij} N_j(w_p) \right)
\phi_i(w,w_p) w_p^2 \, dw_p.
}{(1-f_i(w))(\alpha_i(1-\psi_i(w)))^2\gamma_i(w) \int
( \theta_{ip} N_R(w_p) + \sum_{j} \theta_{ij} N_j(w_p) )
\phi_i(w,w_p) w_p^2 dw_p.}
Here \eqn{N_j(w)} is the abundance density of species \eqn{j} and
\eqn{N_R(w)} is the abundance density of resource.
The overall prefactor \eqn{\gamma_i(w)} determines the predation power of the
predator. It could be interpreted as a search volume and is set with the
\code{\link[=setSearchVolume]{setSearchVolume()}} function. The predation kernel
\eqn{\phi(w,w_p)} is set with the \code{\link[=setPredKernel]{setPredKernel()}} function. The
species interaction matrix \eqn{\theta_{ij}} is set with \code{\link[=setInteraction]{setInteraction()}}
and the resource interaction vector \eqn{\theta_{ip}} is taken from the
\code{interaction_resource} column in \code{\link[=species_params]{species_params()}}.
\eqn{f(w)} is the feeding level calculated with
\code{\link[=getFeedingLevel]{getFeedingLevel()}}. \eqn{\psi(w)} is the proportion of the available energy
that is invested in reproduction instead of growth, obtained with \code{\link[=psi]{psi()}}.

The diffusion integral is normally evaluated efficiently with a fast Fourier
transform, which assumes that the predation kernel depends only on the ratio
of predator to prey size. If a custom predation kernel that depends on
predator and prey size separately has been set with \code{\link[=setPredKernel]{setPredKernel()}}, the
integral is instead evaluated by direct summation over the full predation
kernel, as in \code{\link[=getEncounter]{getEncounter()}}.
}
\references{
Datta, S., Delius, G. W. and Law, R. (2010). A jump-growth model for
predator-prey dynamics: derivation and application to marine ecosystems.
Bulletin of Mathematical Biology, 72(6):1361–1382
}
\seealso{
Other rate functions:
\code{\link[=getEGrowth]{getEGrowth()}},
\code{\link[=getERepro]{getERepro()}},
\code{\link[=getEReproAndGrowth]{getEReproAndGrowth()}},
\code{\link[=getEncounter]{getEncounter()}},
\code{\link[=getFMort]{getFMort()}},
\code{\link[=getFMortGear]{getFMortGear()}},
\code{\link[=getFeedingLevel]{getFeedingLevel()}},
\code{\link[=getFlux]{getFlux()}},
\code{\link[=getFluxGradient]{getFluxGradient()}},
\code{\link[=getMort]{getMort()}},
\code{\link[=getPredMort]{getPredMort()}},
\code{\link[=getPredRate]{getPredRate()}},
\code{\link[=getRDD]{getRDD()}},
\code{\link[=getRDI]{getRDI()}},
\code{\link[=getRates]{getRates()}},
\code{\link[=getResourceMort]{getResourceMort()}}
}
\concept{rate functions}
