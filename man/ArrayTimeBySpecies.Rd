% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayTimeBySpecies-class.R
\name{ArrayTimeBySpecies}
\alias{ArrayTimeBySpecies}
\title{S3 class for time x species arrays}
\usage{
ArrayTimeBySpecies(x, value_name = NULL, units = NULL, params = NULL)
}
\arguments{
\item{x}{A matrix (time x species).}

\item{value_name}{A string giving the human-readable name for the value.}

\item{units}{A string giving the units (e.g. "g", "g/year").}

\item{params}{A \code{MizerParams} object holding the model that created the
values.}
}
\value{
An \code{ArrayTimeBySpecies} object (inherits from \code{matrix} and \code{array}).
}
\description{
Some functions in mizer return two-dimensional arrays (time x species)
holding quantities like biomass, abundance, or yield rate through time.
The \code{ArrayTimeBySpecies} class wraps these arrays to provide convenient
\code{print()}, \code{summary()}, \code{plot()}, and \code{as.data.frame()} methods.
}
\details{
An \code{ArrayTimeBySpecies} object behaves just like a regular matrix for
arithmetic operations and subsetting. It carries these lightweight attributes:
\itemize{
\item \code{value_name} – a human-readable name for the value
(e.g. "Biomass").
\item \code{units} – the units of the value (e.g. "g", "g/year").
\item \code{params} – the \code{MizerParams} object that created the values.
}
}
\examples{
\donttest{
bio <- getBiomass(NS_sim)
is.ArrayTimeBySpecies(bio)
summary(bio)
}
}
\seealso{
\code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=as.data.frame]{as.data.frame()}}, \code{\link[=plot]{plot()}}
}
