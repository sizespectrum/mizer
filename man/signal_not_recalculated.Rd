% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info_signals.R
\name{signal_not_recalculated}
\alias{signal_not_recalculated}
\title{Signal that a rate array was not recalculated because it is frozen}
\usage{
signal_not_recalculated(
  var,
  quantity,
  reset_call,
  derived_from = "species parameters"
)
}
\arguments{
\item{var}{A string naming the slot that was not recalculated.}

\item{quantity}{A string naming the quantity for the user, for example
"metabolic rate".}

\item{reset_call}{A string with the call that recalculates the quantity, for
example "setMetabolicRate(params, reset = TRUE)".}

\item{derived_from}{A string naming the parameters that the quantity would
have been calculated from.}
}
\value{
\code{NULL} invisibly. Called for its side effect of signalling.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Raised by the rate setters when they leave a frozen array alone although the
species parameters say that it should have a different value. It is reported
as a message, and \code{info_level = 0} silences it along with the other
information. Where no handler is collecting, for example when a rate setter
is called directly rather than via \code{\link[=setParams]{setParams()}}, it is shown anyway,
because it may then be all the user hears. The stronger \code{\link[=signal_frozen]{signal_frozen()}}
warning is raised elsewhere, by whoever knows that the user asked for a
change, see \code{\link[=signal_frozen_changes]{signal_frozen_changes()}}.
}
\examples{
with_info_level(
    signal_not_recalculated("metab", "metabolic rate",
                            "setMetabolicRate(params, reset = TRUE)")
)
}
\concept{info signaling functions}
