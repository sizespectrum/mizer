% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{superseded_steady}
\alias{superseded_steady}
\alias{steady}
\alias{projectToSteady}
\title{Superseded names for the steady-state finders}
\usage{
steady(
  params,
  t_max = 100,
  t_per = 1.5,
  dt = 0.1,
  t_save = dt,
  tol = 0.1 * dt,
  amplitude_tol = 0.01,
  amp_rel_tol = 0.01,
  extinction_threshold = 1e-06,
  return_sim = FALSE,
  preserve = c("reproduction_level", "erepro", "R_max"),
  progress_bar = TRUE,
  info_level = default_info_level(),
  method = c("euler", "predictor_corrector", "tr_bdf2")
)

projectToSteady(
  params,
  effort = params@initial_effort,
  distance_func = distanceSSLogN,
  t_per = 1.5,
  t_max = 100,
  dt = 0.1,
  t_save = dt,
  tol = 0.1 * t_per,
  amplitude_tol = 0.01,
  amp_rel_tol = 0.1,
  extinction_threshold = 1e-06,
  return_sim = FALSE,
  progress_bar = TRUE,
  info_level = default_info_level(),
  method = c("euler", "predictor_corrector", "tr_bdf2"),
  ...
)
}
\arguments{
\item{params}{A \linkS4class{MizerParams} object}

\item{t_max}{The maximum number of years to run the simulation. Default is 100.}

\item{t_per}{The simulation is broken up into shorter runs of \code{t_per} years,
after each of which we check for convergence. Default value is 1.5. This
should be chosen as an odd multiple of the timestep \code{dt} in order to be
able to detect period 2 cycles.}

\item{dt}{The time step to use in \code{project()}.}

\item{t_save}{The interval at which a cheap per-species biomass summary is
recorded for limit-cycle detection. Must be a positive multiple of \code{dt} and
a divisor of \code{t_per}. Smaller values resolve the cycle period more finely at
a small extra cost. Default is \code{dt}.}

\item{tol}{The run stops when the number returned by \code{distance_func} for two
states \code{t_per} years apart drops below \code{tol}. Its meaning therefore depends
on the distance function you supply.}

\item{amplitude_tol}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
The minimum relative biomass amplitude for a persistent oscillation to be
reported as a limit cycle rather than treated as an (effectively steady)
fixed point. This is a fraction of mean biomass and is kept separate from
\code{tol} (which measures convergence to a fixed point on a different scale).
Default \code{0.01}.}

\item{amp_rel_tol}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Maximum relative change of amplitude between successive periods for the
cycle to count as settled. Default \code{0.01}.}

\item{extinction_threshold}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
A species is treated as going extinct, stopping the run, once its
reproduction rate (RDD) falls below this fraction of its value at the start
of the run. For example the default \code{1e-6} treats a species as extinct once
its reproduction has collapsed to a millionth of its initial level. Because
it is relative to the initial reproduction, a species that starts with zero
reproduction is flagged immediately, and (in \code{\link[=tuneSteadyState]{tuneSteadyState()}}, where
reproduction is held constant) a healthy species is never flagged.}

\item{return_sim}{If TRUE, the function returns the MizerSim object holding
the result of the simulation run, saved at intervals of \code{t_per}. If FALSE
(default) the function returns a MizerParams object with the "initial"
slots set to the steady state.}

\item{preserve}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Specifies whether the \code{reproduction_level} should be preserved (default)
or the maximum reproduction rate \code{R_max} or the reproductive
efficiency \code{erepro}. See \code{\link[=setBevertonHolt]{setBevertonHolt()}} for an explanation
of the \code{reproduction_level}.}

\item{progress_bar}{A shiny progress object to implement a progress bar in a
shiny app. Default FALSE.}

\item{info_level}{Controls the amount of information messages that are shown.
Higher levels lead to more messages, \code{info_level = 0} gives silence. The
default is taken from the \code{mizer_info_level} option, see
\code{\link[=default_info_level]{default_info_level()}}.}

\item{method}{The numerical method to use for the consumer density update.
See \code{\link[=project]{project()}}.}

\item{effort}{The fishing effort to use throughout. By default the initial
effort stored in \code{params}.}

\item{distance_func}{A function that will be called after every \code{t_per} years
with both the previous and the new state and that should return a number
that in some sense measures the distance between the states. By default
this uses the function \code{\link[=distanceSSLogN]{distanceSSLogN()}} that you can use as a model for your
own distance function.}

\item{...}{Arguments for the chosen solver.

With \code{solver = "project"}: \code{t_max}, \code{t_per}, \code{dt}, \code{t_save}, \code{tol},
\code{amplitude_tol}, \code{amp_rel_tol}, \code{extinction_threshold}, \code{progress_bar} and
\code{method}, all as described in \code{\link[=projectUntilSettled]{projectUntilSettled()}}. Note that \code{tol}
here defaults to \code{0.1 * dt} and measures the largest relative change in
egg production, because the distance function is \code{\link[=distanceMaxRelRDI]{distanceMaxRelRDI()}}.

With \code{solver = "newton"}: \code{residual_tol} (default \code{1e-6}), a tolerance on
the per-capita rate of change passed to \code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}}; \code{maxit}
(default \code{200}); \code{jacobian}, either \code{"update"} (default, the Jacobian is
computed once and then updated cheaply each iteration — \code{nleqslv}'s
\code{"Broyden"}) or \code{"recompute"} (a numerical Jacobian at every iteration —
\code{nleqslv}'s \code{"Newton"}); \code{global}, the globalisation strategy (default
\code{"dbldog"}, a robust double-dogleg trust region); and \code{verbose} to trace
the iterations.}
}
\value{
A \code{MizerParams} object, or a \code{MizerSim} object if
\code{return_sim = TRUE}, in either case carrying the \code{"convergence"} attribute
described in \code{\link[=projectUntilSettled]{projectUntilSettled()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}}

Neither of these names says what distinguishes the two functions, and
\code{projectToSteady()} returns a different class depending on an argument. Both
have been replaced:\tabular{ll}{
   Superseded \tab Use instead \cr
   \code{steady()} \tab \code{\link[=tuneSteadyState]{tuneSteadyState()}} \cr
   \code{projectToSteady()} \tab \code{\link[=findSteadyState]{findSteadyState()}}, or \code{\link[=projectUntilSettled]{projectUntilSettled()}} for the trajectory \cr
}


The distinction the new names carry is what each one holds fixed.
\code{\link[=tuneSteadyState]{tuneSteadyState()}} holds the inputs to the fish dynamics — the reproduction
rate and the resource — at the values you supply while the spectra settle,
and then adjusts the parameters that generate them (\code{erepro}/\code{R_max} and
\code{cc_pp}) so that those held values are steady too. That is what \code{steady()}
always did. \code{\link[=findSteadyState]{findSteadyState()}} changes no parameter and lets reproduction,
the resource and the spectra settle together, which is what
\code{projectToSteady()} did.

Each of the two also gained a \code{solver} argument, so the same job can be done
either by running the dynamics (\code{solver = "project"}, the default and the old
behaviour) or by solving the steady-state equation directly with a
Newton-type root finder (\code{solver = "newton"}), which converges even at a
dynamically unstable steady state.

The \code{return_sim} argument is gone from the new functions:
\code{\link[=tuneSteadyState]{tuneSteadyState()}} and \code{\link[=findSteadyState]{findSteadyState()}} always return a \code{MizerParams}
object and \code{\link[=projectUntilSettled]{projectUntilSettled()}} always returns a \code{MizerSim}.

The old names are however not going away. They are thin wrappers that
reproduce the old behaviour exactly, \code{return_sim} included: they do not warn
and they will keep working, so existing code and old scripts run unchanged.
They are not used anywhere inside mizer and should not be used in new code.
}
\seealso{
\code{\link[=tuneSteadyState]{tuneSteadyState()}}, \code{\link[=findSteadyState]{findSteadyState()}}, \code{\link[=projectUntilSettled]{projectUntilSettled()}}
}
\concept{deprecated}
