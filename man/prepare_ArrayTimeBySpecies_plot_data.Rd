% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayTimeBySpecies-class.R
\name{prepare_ArrayTimeBySpecies_plot_data}
\alias{prepare_ArrayTimeBySpecies_plot_data}
\title{The complete plotting data of a time-by-species array}
\usage{
prepare_ArrayTimeBySpecies_plot_data(
  x,
  species = NULL,
  tlim = c(NA, NA),
  ylim = c(NA, NA),
  total = FALSE,
  background = TRUE,
  log_y = TRUE
)
}
\arguments{
\item{x}{An \code{ArrayTimeBySpecies} object.}

\item{species}{Character vector of species to include, or \code{NULL} for all.}

\item{tlim}{Numeric vector of length two giving the time limits.}

\item{ylim}{Numeric vector of length two giving the value limits. Values
outside them are dropped, as they cannot be seen anyway.}

\item{total}{Whether to append the total, which is the total over every
species the array holds, whatever is drawn.}

\item{background}{Whether background species are included.}

\item{log_y}{Whether the values will be drawn on a logarithmic axis. Only
then are non-positive values dropped: they have no place on a log axis, but
on a linear one they are data like any other, and a quantity that can go
negative — a rate of change, a difference between two models — would
otherwise lose exactly the part of it that is interesting.}
}
\value{
A data frame with \code{Year}, value, \code{Species} and \code{Legend} columns.
}
\description{
The species selection and the background grouping are done in a single pass,
so that no species can be both selected under its own name and appended again
under the \code{"Background"} legend — which is what a separate appending step
used to do to every background species whenever \code{species} was left at its
default of all of them.
}
\keyword{internal}
