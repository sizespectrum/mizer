% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArraySpeciesBySize-class.R
\name{species_size_range_mask}
\alias{species_size_range_mask}
\title{Which size classes lie inside each species' own size range}
\usage{
species_size_range_mask(params, w, species)
}
\arguments{
\item{params}{A \linkS4class{MizerParams} object, or \code{NULL}.}

\item{w}{The size represented by each column, from
\code{get_ArraySpeciesBySize_w()}. Passed in rather than read from \code{params}
because a bin-averaged array is drawn at the bin centres.}

\item{species}{Character vector of the species, one per row.}
}
\value{
A logical matrix (species x size), \code{TRUE} where the size class lies
inside that species' size range.
}
\description{
A rate array is defined on the whole size grid, but a species only occupies
the part of it between its \code{w_min} and \code{w_max}. The values outside that range
are arithmetic rather than biology — the encounter rate a 40 kg Sprat would
have — so \code{plot()} and \code{summary()} leave them out unless asked for them with
\code{all.sizes = TRUE}. This is the single definition of which classes those are,
so that the two cannot disagree about what the array contains.
}
\details{
A species that the model does not know about — a row whose name is not in
\code{species_params}, or any row at all when the array carries no \code{params} — has
no range to be outside of, so all of its size classes are kept.
}
\keyword{internal}
