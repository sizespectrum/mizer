% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MizerParams-class.R
\name{validation_key}
\alias{validation_key}
\title{Fingerprint of the slots that determine the outcome of the repair and
structural validity checks in \code{validParams()}}
\usage{
validation_key(params)
}
\arguments{
\item{params}{A MizerParams object.}
}
\value{
A string.
}
\description{
The fingerprint covers every slot that \code{\link[=repair_params]{repair_params()}} reads or writes and
every slot that the \code{MizerParams} validity function inspects, except for the
values inside the large rate arrays, of which only the dimensions and
dimension names are included. The values in those arrays are checked
unconditionally by \code{\link[=check_finite]{check_finite()}} instead.
}
\details{
The fingerprint is always calculated afresh from the current contents of the
object, never stored on the object itself, so no change to the object can
escape it.
}
\keyword{internal}
