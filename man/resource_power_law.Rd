% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{resource_power_law}
\alias{resource_power_law}
\title{Construct the background resource power-law spectrum}
\usage{
resource_power_law(params, kappa, lambda, w_max = Inf)
}
\arguments{
\item{params}{A MizerParams object whose \code{second_order_w} slot controls the
gating and whose \code{w_full}/\code{dw_full} give the grid.}

\item{kappa}{The coefficient \eqn{\kappa} of the power law.}

\item{lambda}{The exponent so the power law is \eqn{w^{-\lambda}}.}

\item{w_max}{Optional upper cutoff. The power law is taken to be zero at and
above \code{w_max}. Defaults to \code{Inf} (no cutoff).}
}
\value{
A numeric vector (same length as \code{w_full}) of the resource spectrum.
}
\description{
Internal helper returning the auto-calculated resource power law
\eqn{\kappa\, w^{-\lambda}} on the full size grid, optionally truncated at an
upper cutoff \code{w_max}. When the \code{bin_average} entry of the model's
\code{second_order_w} slot is set, the exact bin average of the power law over
each bin is returned instead (with the bin straddling \code{w_max} getting the
partial average), so that the initial resource is the finite-volume cell
average of the background spectrum, consistent with the bin-integrated
encounter convolution that consumes it as a cell average. Otherwise the
left-edge point values are returned, byte-identical to previous mizer.
}
\details{
This is used wherever the background resource spectrum \eqn{\kappa
w^{-\lambda}} is constructed from scratch: the initial resource abundance in
\code{\link[=newMultispeciesParams]{newMultispeciesParams()}} and the temporary prey spectra used to compute the
default \code{gamma}/\code{f0} (\code{\link[=get_gamma_default]{get_gamma_default()}}, \code{\link[=get_f0_default]{get_f0_default()}}) and the
consumer initial abundances (\code{\link[=get_initial_n]{get_initial_n()}}). The bin-averaged resource
capacity and rate are produced directly by \code{\link[=setResource]{setResource()}}.
}
\concept{helper}
\keyword{internal}
