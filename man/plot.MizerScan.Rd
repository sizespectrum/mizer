% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MizerScan-class.R
\name{plot.MizerScan}
\alias{plot.MizerScan}
\title{Plot method for \code{MizerScan} objects}
\usage{
\method{plot}{MizerScan}(
  x,
  species = NULL,
  style = c("ribbon", "envelope", "line"),
  highlight = NULL,
  log_x = FALSE,
  log_y = TRUE,
  log = NULL,
  xlim = c(NA, NA),
  ylim = c(NA, NA),
  y_ticks = 6,
  reference_lines = TRUE,
  mark_max = FALSE,
  show_unsettled = TRUE,
  return_data = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{MizerScan} object.}

\item{species}{The species to show. By default all series in the scan.}

\item{style}{One of \code{"ribbon"} (default: the average as a line inside the
band), \code{"envelope"} (lines along the edges of the band, no average) or
\code{"line"} (no band).}

\item{highlight}{Name or vector of names of the species to be highlighted
with a thicker line.}

\item{log_x, log_y, log}{Whether to use logarithmic axes, see \code{\link[=parsePlotLog]{parsePlotLog()}}.}

\item{xlim, ylim}{Numeric vectors of length two giving the axis limits. Use
\code{NA} to refer to the existing minimum or maximum.}

\item{y_ticks}{The approximate number of ticks desired on the y axis.}

\item{reference_lines}{Whether to draw the reference lines stored in the
scan, or a named numeric vector of x positions to draw instead.}

\item{mark_max}{Whether to mark, for each series, the scanned value at which
the measured quantity is largest. See \code{\link[=MizerScan]{MizerScan()}}.}

\item{show_unsettled}{Whether to mark the scan values where the model did not
settle onto an attractor.}

\item{return_data}{Whether to return the data frame used for the plot instead
of the plot itself.}

\item{...}{Unused.}
}
\value{
A ggplot2 object, unless \code{return_data = TRUE}, in which case the data
frame used for the plot is returned.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Draws the result of a \code{\link[=scanModel]{scanModel()}} run: the measured quantity against the
quantity that was scanned, with a band showing the range the quantity takes
over the attractor wherever that attractor is not a fixed point.
}
\details{
A model that settles on a fixed point contributes a single value, so the band
has zero width there. A model that settles on a limit cycle contributes the
average over one period as the line and the range over that period as the
band, so a Hopf bifurcation shows up as the scan value at which the band
opens up.

Scan values where the model reached neither a fixed point nor a limit cycle
within the time allowed are marked with a cross, because the value plotted
there is only an average over the last few years of a run that was still
changing.
}
\examples{
\donttest{
scan <- scanModel(NS_params, scan_values = seq(0, 1, 0.25),
                  set_func = scanEffort(), species = c("Cod", "Herring"))
plot(scan)
plot(scan, style = "envelope", mark_max = TRUE)
}
}
\seealso{
\code{\link[=scanModel]{scanModel()}}, \code{\link[=MizerScan]{MizerScan()}}, \link{plotting_functions}

Other scan functions:
\code{\link[=MizerScan]{MizerScan()}},
\code{\link[=plotYieldVsF]{plotYieldVsF()}},
\code{\link[=scanEffort]{scanEffort()}},
\code{\link[=scanModel]{scanModel()}}
}
\concept{scan}
\concept{scan functions}
