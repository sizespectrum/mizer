% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayTimeBySpeciesBySize-class.R
\name{ArrayTimeBySpeciesBySize}
\alias{ArrayTimeBySpeciesBySize}
\alias{is.ArrayTimeBySpeciesBySize}
\title{S3 class for time x species x size arrays}
\usage{
ArrayTimeBySpeciesBySize(
  x,
  value_name = NULL,
  units = NULL,
  params = NULL,
  representation = c("point", "average")
)

is.ArrayTimeBySpeciesBySize(x)
}
\arguments{
\item{x}{A 3D array (time x species x size). For
\code{is.ArrayTimeBySpeciesBySize()}, any object to test.}

\item{value_name}{A string giving the human-readable name for the value.}

\item{units}{A string giving the units (e.g. "1/year").}

\item{params}{A \code{MizerParams} object. Used for species colours, linetypes,
and size ranges in the \code{plot()} and \code{animateSpectra()} methods.}

\item{representation}{Either \code{"point"} (the default) for a quantity sampled
at the grid nodes, or \code{"average"} for a finite-volume bin average, which is
then drawn at the geometric bin centre when the model uses second-order
bin-averaging (\code{second_order_w[["bin_average"]]}). See
\code{\link[=ArraySpeciesBySize]{ArraySpeciesBySize()}}.}
}
\value{
An \code{ArrayTimeBySpeciesBySize} object (inherits from \code{array}).

\code{is.ArrayTimeBySpeciesBySize()} returns \code{TRUE} if \code{x} is an
\code{ArrayTimeBySpeciesBySize} object, \code{FALSE} otherwise.
}
\description{
Some functions in mizer return three-dimensional arrays (time x species x
size) holding quantities like fishing mortality, feeding level, or predation
mortality through time. The \code{ArrayTimeBySpeciesBySize} class wraps these
arrays to provide convenient \code{print()}, \code{summary()}, \code{plot()},
\code{animate()}, and \code{as.data.frame()} methods.
}
\details{
An \code{ArrayTimeBySpeciesBySize} object behaves just like a regular array for
arithmetic operations and subsetting. It carries these lightweight attributes:
\itemize{
\item \code{value_name} – a human-readable name for the value
(e.g. "Fishing mortality").
\item \code{units} – the units of the value (e.g. "1/year").
\item \code{params} – the \code{MizerParams} object that the value was computed from.
}
}
\examples{
\donttest{
fmort <- getFMort(NS_sim)
is.ArrayTimeBySpeciesBySize(fmort)
summary(fmort)
plot(fmort, time = 2007)
}
}
\seealso{
\code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=as.data.frame]{as.data.frame()}}, \code{\link[=plot]{plot()}},
\code{\link[=animateSpectra]{animateSpectra()}}
}
