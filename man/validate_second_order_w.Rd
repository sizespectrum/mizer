% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/second_order_w.R
\name{validate_second_order_w}
\alias{validate_second_order_w}
\title{Apply a \code{second_order_w} value to the current slot list}
\usage{
validate_second_order_w(current, value)
}
\arguments{
\item{current}{The current \code{second_order_w} slot list (with entries \code{flux}
and \code{bin_average}).}

\item{value}{The value to apply, as described above.}
}
\value{
The updated \code{second_order_w} list.
}
\description{
Internal helper that validates a \code{second_order_w} \code{value} (a single logical,
a single flux scheme name, or a named vector with entries \code{flux} and/or
\code{bin_average}) and returns the updated named list. Shared by the
\code{\link{second_order_w<-}} setter and by the model constructors (e.g.
\code{\link[=newMultispeciesParams]{newMultispeciesParams()}}), which set the slot directly before the rest of
the parameters are computed so that the bin-averaged constructions pick up
the flag, without the setter's extra \code{\link[=setParams]{setParams()}} call.
}
\concept{helper}
\keyword{internal}
