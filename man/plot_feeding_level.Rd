% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_feeding_level}
\alias{plot_feeding_level}
\title{Build the feeding-level plot}
\usage{
plot_feeding_level(
  params,
  feed,
  species,
  highlight,
  all.sizes,
  include_critical,
  log_x,
  log_y,
  wlim,
  llim,
  size_axis,
  return_data
)
}
\arguments{
\item{params}{A MizerParams object.}

\item{feed}{Array of feeding levels (species by size).}

\item{species}{The species to be plotted.}

\item{highlight}{Name or vector of names of species to be highlighted.}

\item{all.sizes}{If \code{FALSE}, feeding levels outside each species' size range
are removed.}

\item{include_critical}{Whether to also plot the critical feeding level.}

\item{log_x, log_y}{Logical flags for log10 axes.}

\item{wlim, llim}{Numeric vectors of length two giving the weight and length
limits.}

\item{size_axis}{Either \code{"w"} (weight) or \code{"l"} (length).}

\item{return_data}{If \code{TRUE}, return the plotting data frame instead of the
plot.}
}
\value{
A \code{mizer_plot} (ggplot2) object, or the plotting data frame if
\code{return_data = TRUE}.
}
\description{
Internal worker shared by the \code{\link[=plotFeedingLevel]{plotFeedingLevel()}} methods. It assembles the
plotting data, optionally adds the critical feeding level, restricts to each
species' size range, converts to a length axis if requested and draws the
plot.
}
\keyword{internal}
