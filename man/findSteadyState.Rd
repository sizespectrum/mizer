% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steadyState.R
\name{findSteadyState}
\alias{findSteadyState}
\title{Find the steady state of a model}
\usage{
findSteadyState(
  params,
  solver = c("project", "newton"),
  effort = params@initial_effort,
  info_level = default_info_level(),
  ...
)
}
\arguments{
\item{params}{A \linkS4class{MizerParams} object}

\item{solver}{The solver to use: \code{"project"} to run the dynamics until they
settle, \code{"newton"} to solve the steady-state equation directly. See
\emph{Choosing a solver}.}

\item{effort}{The fishing effort to use throughout. By default the initial
effort stored in \code{params}.}

\item{info_level}{Controls the amount of information messages that are shown.
Higher levels lead to more messages, \code{info_level = 0} gives silence. The
default is taken from the \code{mizer_info_level} option, see
\code{\link[=default_info_level]{default_info_level()}}.}

\item{...}{Arguments for the chosen solver.

With \code{solver = "project"}: \code{distance_func}, \code{t_max}, \code{t_check}, \code{dt},
\code{distance_tol}, \code{residual_tol}, \code{amplitude_tol}, \code{amp_rel_tol},
\code{extinction_threshold}, \code{progress_bar} and \code{method}, all as described in
\code{\link[=projectUntilSettled]{projectUntilSettled()}}. There is no \code{t_save}, because no trajectory is
returned.

With \code{solver = "newton"}: \code{extinction_floor} (default \code{1e-6}), the relative
abundance below which a species counts as extinct, plus \code{solver_tol},
\code{maxit}, \code{jacobian}, \code{global} and \code{verbose} as described in
\code{\link[=tuneSteadyState]{tuneSteadyState()}}.}
}
\value{
A \code{MizerParams} object with the initial state replaced by the steady
state found and no parameter changed. It carries a \code{"convergence"}
attribute describing the solution found; see \code{\link[=projectUntilSettled]{projectUntilSettled()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Puts the model onto a steady state of its own dynamics, changing no
parameter: the reproduction rate, the resource and the consumer spectra all
settle together at whatever the parameters you already have imply.
}
\details{
This is the counterpart of \code{\link[=tuneSteadyState]{tuneSteadyState()}}, which instead holds the
reproduction rate and the resource at the values you supplied and adjusts
\code{erepro}/\code{R_max} and \code{cc_pp} to make those values steady. Use this function
when the parameters are the thing you want to keep — when asking what state a
given model settles into, for instance under a changed fishing effort — and
\code{\link[=tuneSteadyState]{tuneSteadyState()}} while calibrating.

Nothing being held fixed means the search has more ways to end badly. With
\code{solver = "project"} the run can settle on a limit cycle or drive a species
extinct rather than reach a fixed point, and with either solver reproduction
can collapse. Check the \code{"convergence"} attribute rather than assuming a
fixed point was reached; see the section below.
}
\section{Choosing a solver}{
\code{solver = "project"} (the default) runs the dynamics until they settle, via
\code{\link[=projectUntilSettled]{projectUntilSettled()}}, and takes the final state. It is exactly that
function with the trajectory thrown away; call \code{\link[=projectUntilSettled]{projectUntilSettled()}}
instead if you want to watch the approach.

\code{solver = "newton"} solves the steady-state equation directly with a
Newton-type root finder from the \code{nleqslv} package, so it converges even when
the steady state is dynamically \strong{unstable}, where the time-stepping solver
diverges away from it. This is the natural entry point for a stability
analysis with \code{\link[=getStability]{getStability()}}.

The Newton solver treats the resource densities as unknowns alongside the
fish and appends the resource steady-state equation to the system, so the
resource density and the feeding levels it implies are self-consistent even
where consumers are satiated. That equation is written for the default
semichemostat resource dynamics, so \code{solver = "newton"} stops with an error
for any other \code{resource_dynamics}; use \code{solver = "project"} there.

The Newton iteration also needs the residual \eqn{F(N)} to be continuous. A
custom rate function registered with \code{\link[=setRateFunction]{setRateFunction()}} that jumps as a
function of the abundances makes \eqn{F} discontinuous, and where the
equilibrium lies on the switching threshold there is no root at all, because
neither branch is in equilibrium there. The solver then stalls (\code{nleqslv}
termination code 3) and returns an iterate pinned to the threshold. See
\href{https://sizespectrum.org/mizer/articles/discontinuous_rates.html}{Discontinuous rate functions}.

It also respects the active transport scheme: if the experimental
second-order scheme is enabled (see \code{\link[=second_order_w]{second_order_w()}}) it solves the
steady-state equation of that scheme. With the van Leer reconstruction the
residual is only Lipschitz, so the iteration converges to a fixed point of the
dynamics but not to machine precision. The unlimited \code{"centred"}
reconstruction admits an undamped odd-even mode at a steady state with no
physical diffusion, giving an ill-conditioned steady-state Jacobian for which
the solver is not expected to converge.
}

\section{What you get back may not be a steady state}{


The stopping criterion is a proxy. It says that two states \code{t_per} years
apart differ by less than \code{distance_tol} on whatever scale the criterion is
measured on; it does not say that the state reached is a fixed point. There
are four ways the returned object can fail to be one:
\itemize{
\item the run converged on its own scale while the biomasses are still visibly
drifting (\code{termination = "distance_tolerance"});
\item the run reached \code{t_max} without converging
(\code{termination = "time_limit"});
\item the run settled on a limit cycle (\code{termination = "cycle_detected"}), in
which case the state stored is one point on that cycle;
\item the run stopped because a species was going extinct
(\code{termination = "extinction"}).
}

So treat the result as a claim to be checked rather than as a guarantee:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{attr(params, "convergence")$attractor  # "fixed_point", "limit_cycle" or NA
attr(params, "convergence")$residual   # largest biomass drift, in 1/year
isSteady(params)                       # TRUE if within tolerance
summary(params)                        # includes the biomass-drift verdict
plot(getSteadyResidual(params))        # which species, and at which sizes
}\if{html}{\out{</div>}}

\code{attractor} is the field that answers the question: it is \code{"fixed_point"}
only where the measured biomass drift is within \code{residual_tol}, so it
cannot be satisfied by a distance function that has merely gone quiet.
\code{termination} says how the run ended and \code{converged} whether the solver met
its own criterion; neither is a claim about the state. The last line says
\emph{where} the model is not steady, which is the one to reach for when it is
not: a model that is off steady state is usually off in one species or one
part of the size range, and the plot names it. See \code{\link[=getSteadyResidual]{getSteadyResidual()}}
for why the verdict is phrased in terms of biomass drift rather than the
largest per-capita rate.

The messages this function prints say the same thing — a converged run
whose biomasses are still moving reports the drift and adds "Reduce the
tolerance on the distance function to converge further." — but they are
suppressed by \code{info_level = 0}, so in a script the \code{"convergence"}
attribute is the reliable check.

Finally, a genuine fixed point need not be a \emph{stable} one. Use
\code{\link[=getStability]{getStability()}} to find out, and \code{solver = "newton"} to converge onto a
fixed point that the dynamics themselves would run away from.
}

\examples{
\donttest{
params <- findSteadyState(NS_params, solver = "newton")
plotSpectra(params)
}
}
\seealso{
\code{\link[=tuneSteadyState]{tuneSteadyState()}}, \code{\link[=projectUntilSettled]{projectUntilSettled()}}, \code{\link[=isSteady]{isSteady()}},
\code{\link[=getSteadyResidual]{getSteadyResidual()}}, \code{\link[=getStability]{getStability()}}
}
