% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArraySpeciesBySize-class.R
\name{ArraySpeciesBySize_plot_data}
\alias{ArraySpeciesBySize_plot_data}
\title{The complete plotting data of a species-by-size array}
\usage{
ArraySpeciesBySize_plot_data(
  x,
  species = NULL,
  all.sizes = FALSE,
  wlim = c(NA, NA),
  llim = c(NA, NA),
  total = FALSE,
  background = TRUE,
  size_axis = "w",
  per_log_size = NULL
)
}
\arguments{
\item{x}{An \code{ArraySpeciesBySize} object.}

\item{species}{Character vector of species to include, or \code{NULL} for all.}

\item{all.sizes}{If \code{FALSE}, values outside a species' size range are
removed.}

\item{wlim}{Numeric vector of length two giving the weight limits.}

\item{llim}{Numeric vector of length two giving the length limits, applied
only on a length axis.}

\item{total}{Whether to append the total line, see \code{\link[=total_contributors]{total_contributors()}}.}

\item{background}{Whether background species are included.}

\item{size_axis}{Either \code{"w"} (weight) or \code{"l"} (length).}

\item{per_log_size}{Whether to express a density per logarithmic size.}
}
\value{
A data frame with the size coordinate in its first column, the values
in its second, and \code{Species} and \code{Legend} columns.
}
\description{
Everything a plot of an \code{ArraySpeciesBySize} needs, prepared once: the
species selection, the background grouping, the masking of sizes outside a
species' own range, the weight limits, the conversion of the values and of
the size coordinate onto the requested axis, the total line, and the length
limits.
}
\details{
All of it uses the array's \emph{own} \code{params}. That matters for the comparison
plots, where the two operands may come from different models: a length axis
and a density Jacobian are both built from the weight-length relationship of
the model the values came from, so preparing the second array with the first
one's parameters would put it in the wrong place on the axis. Each operand is
therefore prepared here, on its own, and the comparison renderers receive
data that is already on the axis it will be drawn against.
}
\keyword{internal}
