% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ArrayTimeBySpeciesBySize-class.R,
%   R/animateSpectra.R
\name{animate.ArrayTimeBySpeciesBySize}
\alias{animate.ArrayTimeBySpeciesBySize}
\alias{animate}
\alias{animate.MizerSim}
\alias{animateSpectra}
\title{Animate size-dependent quantities through time}
\usage{
\method{animate}{ArrayTimeBySpeciesBySize}(
  x,
  species = NULL,
  time_range = NULL,
  log_x = TRUE,
  wlim = c(NA, NA),
  ylim = c(NA, NA),
  log_y = TRUE,
  size_axis = c("w", "l"),
  total = FALSE,
  background = TRUE,
  frame_duration = 500,
  transition_duration = frame_duration,
  easing = "linear",
  ...
)

animate(x, ...)

\method{animate}{MizerSim}(
  x,
  species = NULL,
  time_range = NULL,
  log_x = TRUE,
  log_y = TRUE,
  wlim = c(NA, NA),
  ylim = c(NA, NA),
  size_axis = c("w", "l"),
  power = 1,
  total = FALSE,
  resource = TRUE,
  background = TRUE,
  frame_duration = 500,
  transition_duration = frame_duration,
  easing = "linear",
  ...
)

animateSpectra(sim, ...)
}
\arguments{
\item{x}{A \code{MizerSim} or \code{ArrayTimeBySpeciesBySize} object.}

\item{species}{Name or vector of names of the species to be plotted. By
default all species are plotted.}

\item{time_range}{The time range to animate over. Either a vector of time
values to include, or a length-two vector giving the min and max of the
range. Default is the entire time range of \code{x}.}

\item{log_x}{If \code{TRUE} (default), use a log10 x-axis for body size.}

\item{wlim}{A numeric vector of length two providing lower and upper limits
for the body-size (x) axis. Use \code{NA} to refer to the existing minimum or
maximum.}

\item{ylim}{A numeric vector of length two providing lower and upper limits
for the value (y) axis. Use \code{NA} to refer to the existing minimum or
maximum. Limits are applied as Plotly axis ranges, so points outside the
limits are clipped by the viewport rather than removed from the animation
frames.}

\item{log_y}{If \code{TRUE} (default), use a log10 y-axis.}

\item{size_axis}{Whether to plot size as weight (\code{"w"}, default) or length
(\code{"l"}), using the allometric weight-length relationship.}

\item{total}{A boolean value that determines whether the total over all
selected species is plotted as an additional trace called \code{"Total"}.
Default is \code{FALSE}.}

\item{background}{A boolean value that determines whether background species
are included. Ignored if the model does not contain background species.
Default is \code{TRUE}.}

\item{frame_duration}{Duration in milliseconds for which each saved frame is
displayed. Default is 500.}

\item{transition_duration}{Duration in milliseconds of the interpolation
between frames. Use \code{transition_duration = 0} to step directly from one
saved frame to the next. Default is \code{frame_duration}.}

\item{easing}{The Plotly easing function to use when interpolating between
frames. Default is \code{"linear"}. Available options are \code{"linear"}, \code{"quad"},
\code{"cubic"}, \code{"sin"}, \code{"exp"}, \code{"circle"}, \code{"elastic"}, \code{"back"},
\code{"bounce"}, and each of those with suffix \code{"-in"}, \code{"-out"}, or
\code{"-in-out"} appended, for example \code{"cubic-in-out"}.}

\item{...}{Additional arguments passed to the method.}

\item{power}{The abundance is plotted as the number density times the weight
raised to \code{power}. The default \code{power = 1} gives the biomass
density, whereas \code{power = 2} gives the biomass density with respect
to logarithmic size bins. Only applies to \code{MizerSim}.}

\item{resource}{A boolean value that determines whether resource is included.
If \code{TRUE}, the resource spectrum is plotted as an additional trace called
\code{"Resource"}. Default is \code{TRUE}. Only applies to \code{MizerSim}.}

\item{sim}{A \code{MizerSim} object.}
}
\value{
A plotly object with one animated line trace per plotted group. Use
the play button or the slider to step through time.
}
\description{
Creates an interactive plotly animation in which a play button steps through
time, drawing one line per species at each frame.
}
\details{
The function dispatches on the class of \code{x}:
\itemize{
\item \strong{\code{MizerSim}} — animates the community abundance spectra (number density
or biomass density vs body size). Resource, background species, and a
community total can be added via the \code{resource}, \code{background}, and \code{total}
arguments. The \code{power} argument controls whether the y-axis shows number
density (\code{power = 0}), biomass density (\code{power = 1}, default), or biomass
density in logarithmic size bins (\code{power = 2}). Both axes are log10 by
default and can each be switched to linear with \code{log_x = FALSE} or
\code{log_y = FALSE}.
\item \strong{\code{ArrayTimeBySpeciesBySize}} — animates any per-species, size-resolved
quantity returned by a \code{MizerSim} accessor, such as \code{\link[=getFMort]{getFMort()}},
\code{\link[=getFeedingLevel]{getFeedingLevel()}}, or \code{\link[=getPredMort]{getPredMort()}}. Both axes are log10 by default
and can each be switched to linear with \code{log_x = FALSE} or \code{log_y = FALSE}.
Background species and a species total can be added via the \code{background}
and \code{total} arguments.
}

Species linecolours and linetypes follow \code{params@linecolour} and
\code{params@linetype}.

\code{animateSpectra()} is retained as a backward-compatible alias.
}
\examples{
\donttest{
# Animate biomass density spectra, showing only sizes above 0.1 g
animate(NS_sim, power = 2, wlim = c(0.1, NA), time_range = 1997:2007)

# Animate fishing mortality through time
animate(getFMort(NS_sim))

# Animate feeding level for two species only
animate(getFeedingLevel(NS_sim), species = c("Cod", "Herring"))
}
}
\seealso{
Other plotting functions:
\code{\link[=addPlot]{addPlot()}},
\code{\link{plot}},
\code{\link[=plot2]{plot2()}},
\code{\link[=plotBiomass]{plotBiomass()}},
\code{\link[=plotCDF]{plotCDF()}},
\code{\link[=plotCDF2]{plotCDF2()}},
\code{\link[=plotDiet]{plotDiet()}},
\code{\link[=plotFMort]{plotFMort()}},
\code{\link[=plotFeedingLevel]{plotFeedingLevel()}},
\code{\link[=plotGrowthCurves]{plotGrowthCurves()}},
\code{\link{plotMizerParams}},
\code{\link{plotMizerSim}},
\code{\link[=plotPredMort]{plotPredMort()}},
\code{\link[=plotRelative]{plotRelative()}},
\code{\link[=plotSpectra]{plotSpectra()}},
\code{\link[=plotSpectra2]{plotSpectra2()}},
\code{\link[=plotSpectraRelative]{plotSpectraRelative()}},
\code{\link[=plotYield]{plotYield()}},
\code{\link[=plotYieldGear]{plotYieldGear()}},
\code{\link{plotting_functions}}
}
\concept{plotting functions}
