% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upgrade.R
\name{runExtensionUpgrades}
\alias{runExtensionUpgrades}
\title{Run the registered extension upgrade methods on an object}
\usage{
runExtensionUpgrades(params)
}
\arguments{
\item{params}{A MizerParams object.}
}
\value{
The object with extension migrations applied and stamps refreshed.
}
\description{
For each extension recorded in the object's \verb{@extensions} slot (processed
innermost-first) whose installed package version is newer than the recorded
stamp (or whose stamp is missing), calls the extension's \code{upgrade} method if
one is registered, then records the installed version as the new stamp. The
core mizer upgrade is \emph{not} run here; see \code{\link[=upgrade.MizerParams]{upgrade.MizerParams()}}.
}
\details{
Extension upgrade methods are looked up with
\code{getS3method("upgrade", name, optional = TRUE)}, must perform only their own
migration, must be idempotent, and must not call \code{NextMethod()}.
}
\keyword{internal}
