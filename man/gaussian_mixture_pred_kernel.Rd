% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pred_kernel_funcs.R
\name{gaussian_mixture_pred_kernel}
\alias{gaussian_mixture_pred_kernel}
\title{Gaussian-mixture predation kernel}
\usage{
gaussian_mixture_pred_kernel(ppmr, kernel_p, kernel_mean, kernel_sd)
}
\arguments{
\item{ppmr}{A vector of predator/prey mass ratios.}

\item{kernel_p}{A numeric vector of relative component proportions.}

\item{kernel_mean}{A numeric vector of component means on the log
predator/prey mass-ratio scale.}

\item{kernel_sd}{A numeric vector of positive component standard deviations.}
}
\value{
A vector giving the value of the predation kernel at each of the
predator/prey mass ratios in the \code{ppmr} argument.
}
\description{
A predation kernel for which the log predator/prey mass ratio follows a
mixture of Gaussian distributions.
}
\details{
Writing the predator mass as \eqn{w}, the prey mass as \eqn{w_p}, and
\eqn{x = \ln(w / w_p)}, the feeding kernel is
\deqn{
\phi_i(w, w_p) = \sum_j a_{ij}
\exp\left[-\frac{(x - \mu_{ij})^2}{2\sigma_{ij}^2}\right],
\qquad
a_{ij} = \frac{p_{ij}/\sigma_{ij}}
{\sum_k p_{ik}/\sigma_{ik}}.
}
for predator/prey mass ratios greater than or equal to one, and zero for
smaller ratios.

This is proportional to the Gaussian-mixture probability density with
mixing proportions \eqn{p_{ij}}, means \eqn{\mu_{ij}}, and standard
deviations \eqn{\sigma_{ij}}. The scaling makes the sum of the component
peak heights equal to one. Consequently the kernel is at most one, and a
one-component mixture is identical to \code{\link[=lognormal_pred_kernel]{lognormal_pred_kernel()}} with
\code{beta = exp(kernel_mean)} and \code{sigma = kernel_sd}.

The three component parameters are vectors of equal length. When this
function is selected in a species parameter data frame, they should be held
in the list-columns \code{kernel_p}, \code{kernel_mean}, and \code{kernel_sd}. The values in
\code{kernel_p} must be non-negative with at least one positive value, but they do
not need to sum to one because they are normalised by the function.
}
\examples{
ppmr <- exp(seq(0, 12, length.out = 200))
phi <- gaussian_mixture_pred_kernel(
    ppmr,
    kernel_p = c(0.3, 0.7),
    kernel_mean = c(4, 8),
    kernel_sd = c(0.8, 1.5)
)
plot(ppmr, phi, type = "l", log = "x")
}
\seealso{
\code{\link[=setPredKernel]{setPredKernel()}}

Other predation kernel:
\code{\link[=box_pred_kernel]{box_pred_kernel()}},
\code{\link[=lognormal_pred_kernel]{lognormal_pred_kernel()}},
\code{\link[=power_law_pred_kernel]{power_law_pred_kernel()}},
\code{\link[=truncated_lognormal_pred_kernel]{truncated_lognormal_pred_kernel()}}
}
\concept{predation kernel}
