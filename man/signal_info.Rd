% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info_signals.R
\name{signal_info}
\alias{signal_info}
\title{Signal information about a choice mizer made}
\usage{
signal_info(
  var,
  message,
  level = 3,
  severity = c("info", "warning"),
  unhandled = c("drop", "show"),
  class = character()
)
}
\arguments{
\item{var}{A string naming the quantity the report is about.}

\item{message}{The message to give the user.}

\item{level}{How important the report is. Level 1 is important enough to
survive \code{info_level = 1}, level 3 is chatter that only the default
\code{info_level = 3} shows.}

\item{severity}{\code{"info"} to report as a message, \code{"warning"} to report as a
warning. Use \code{"warning"} when the user asked for something that is not
happening, because a message can be, and on the
\code{\link[=species_params<-]{species_params<-()}} path is, suppressed.}

\item{unhandled}{What to do when no handler is collecting, for example
because a rate setter was called directly rather than through
\code{\link[=setParams]{setParams()}}. \code{"drop"} says nothing, which suits chatter that only makes
sense as part of a report about a whole model. \code{"show"} reports it there
and then, at the same severity: a message for \code{"info"} and a warning for
\code{"warning"}.}

\item{class}{Further classes to give the condition, for code that wants to
catch a particular kind of report.}
}
\value{
\code{NULL} invisibly. Called for its side effect of signalling.
}
\description{
Raises the condition that \code{\link[=with_info_level]{with_info_level()}} collects. This is the way for
mizer, and for anything extending it, to tell the user about a default it
filled in, an input it adjusted or an instruction it could not carry out,
without deciding on its own how loudly to say it: the handler installed by
whichever function the user actually called does that.
}
\details{
Progress reports are the one thing that does not belong here: they have to
appear while the work is going on, and these are collected and given at the
end.
}
\examples{
# With nothing collecting, a `"drop"` report says nothing at all ...
signal_info("h", "Using a default for `h`.")

# ... whereas `unhandled = "show"` reports it there and then.
signal_info("h", "Using a default for `h`.", unhandled = "show")

# Normally it is raised inside a call whose body is wrapped in
# `with_info_level()`, which is what decides whether to show it.
with_info_level(signal_info("h", "Using a default for `h`."))
}
\concept{helper}
