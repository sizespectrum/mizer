% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_functions.R
\name{flux_from_rates}
\alias{flux_from_rates}
\title{Assemble the flux matrix from growth, diffusion and recruitment rates}
\usage{
flux_from_rates(params, n, g, d, rdd, power = 0)
}
\arguments{
\item{params}{A valid \code{MizerParams} object.}

\item{n}{A matrix of species abundances (species x size).}

\item{g}{Growth rate matrix (species x size), as from \code{\link[=getEGrowth]{getEGrowth()}}.}

\item{d}{Diffusion rate matrix (species x size), as from \code{\link[=getDiffusion]{getDiffusion()}}.}

\item{rdd}{Density-dependent reproduction rate vector (one per species), as
from \code{\link[=getRDD]{getRDD()}}.}

\item{power}{The flux at weight \eqn{w} is multiplied by \eqn{w} raised to
\code{power}. The default \code{power = 0} leaves the flux of individuals unchanged.}
}
\value{
A plain species x size matrix of fluxes (no mizer array class).
}
\description{
Internal helper holding the arithmetic shared by \code{getFlux.MizerParams} and
\code{getFlux.MizerSim}. Keeping it separate lets the \code{MizerSim} method resolve the
rate functions once and reuse them across all saved time steps.
}
\keyword{internal}
