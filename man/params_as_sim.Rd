% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MizerSim-class.R
\name{params_as_sim}
\alias{params_as_sim}
\title{A MizerSim holding only the state stored in a MizerParams}
\usage{
params_as_sim(params, t = 0)
}
\arguments{
\item{params}{A MizerParams object.}

\item{t}{The time to label the single time step with.}
}
\value{
A MizerSim object with one time step holding the initial state of
\code{params}.
}
\description{
Wraps the single state in a \code{MizerParams} object into a \code{MizerSim} with one
time step, so that a function written against a \code{MizerSim} can be applied to
it without projecting.
}
\details{
This exists for analyses such as \code{\link[=scanModel]{scanModel()}} that measure a quantity with a
user-supplied function of a \code{MizerSim}. When the model has settled on a fixed
point there is nothing to project: the state does not change, so a snapshot
of it carries all the information a longer run would. Every slot that a
summary function might read has to be filled, because \code{\link[=MizerSim]{MizerSim()}} initialises
them all to \code{NA} and, for example, \code{getYield()} reads \code{sim@effort} and would
otherwise return \code{NA} without complaint.

Note that the result has a single time step, so a function that needs more
than one — anything taking a difference through time — cannot be applied to
it.
}
\keyword{internal}
