% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info_signals.R
\name{signal_gear_params_changes}
\alias{signal_gear_params_changes}
\title{Signal a gear parameter changed through the given species parameters}
\usage{
signal_gear_params_changes(changed)
}
\arguments{
\item{changed}{A named list with one entry per changed column, or a
character vector of the changed column names.}
}
\value{
\code{NULL} invisibly. Called for its side effect of signalling.
}
\description{
Mizer looks for the gear parameters in the gear parameter table, which is
read only when the model is built, so changing one of them through the
species parameters does not reach the model. This is one of the diagnostics
that only \code{\link[=given_species_params<-]{given_species_params<-()}} gives; \code{\link[=species_params<-]{species_params<-()}} stays
quiet, see there.
}
\details{
\code{yield_observed} is deliberately not among them. It looks like a gear
parameter but is read straight out of the species parameters by
\code{\link[=matchYields]{matchYields()}}, \code{\link[=calibrateYield]{calibrateYield()}} and \code{\link[=plotYieldObservedVsModel]{plotYieldObservedVsModel()}}, and
both setters put it there, so a change to it does take effect.
}
\concept{helper}
