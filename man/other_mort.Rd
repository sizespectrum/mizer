% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension.R
\name{other_mort}
\alias{other_mort}
\alias{other_mort<-}
\alias{other_encounter}
\alias{other_encounter<-}
\title{Extra contributions to the mortality and encounter rates}
\usage{
other_mort(params)

other_mort(params) <- value

other_encounter(params)

other_encounter(params) <- value
}
\arguments{
\item{params}{A MizerParams object}

\item{value}{A named list of function names, or \code{NULL} to remove all
contributions that do not belong to a component. You choose the names, but
they must be unique and must not clash with the name of a component.}
}
\value{
A named list with the names of the functions contributing to the
rate, excluding any that belong to a component.
}
\description{
Besides the rates that mizer calculates itself, a model can carry extra
contributions to the mortality rate and to the encounter rate. Each
contribution is an R function, registered here by name, that mizer calls at
every time step: \code{\link[=getMort]{getMort()}} adds the result of every function listed in
\code{other_mort(params)} to the mortality rate and \code{\link[=getEncounter]{getEncounter()}} adds the
result of every function listed in \code{other_encounter(params)} to the encounter
rate.
}
\details{
Use these when the extra contribution depends on the state of the model, as
a starvation mortality or a density-dependent mortality does. A contribution
that is simply a fixed array is better set with \code{\link[=ext_mort]{ext_mort()}} or
\code{\link[=ext_encounter]{ext_encounter()}}, which is what mizer's own external mortality and external
encounter use.

Assigning \code{NULL} to an entry removes that contribution:

\if{html}{\out{<div class="sourceCode">}}\preformatted{other_mort(params)[["starvation"]] <- NULL
}\if{html}{\out{</div>}}
}
\section{How your function is called}{

Each registered function is called as

\if{html}{\out{<div class="sourceCode">}}\preformatted{fun(params, n, n_pp, n_other, t, component, ...)
}\if{html}{\out{</div>}}

and has to return an array with the same dimensions as the rate it
contributes to, that is species x size. The \code{component} argument holds the
name under which the function is registered, so a single implementation can
serve several entries. Always give your function a \code{...} argument so that it
tolerates being passed arguments it does not use.

Make sure the function depends \emph{continuously} on the abundances, for the
reasons set out in \code{\link[=setRateFunction]{setRateFunction()}}.
}

\section{Contributions belonging to a component}{

A component set up with \code{\link[=setComponent]{setComponent()}} can contribute to these rates
through that function's \code{mort_fun} and \code{encounter_fun} arguments. Such an
entry belongs to its component: \code{\link[=setComponent]{setComponent()}} sets it, \code{\link[=removeComponent]{removeComponent()}}
removes it and \code{\link[=getComponent]{getComponent()}} reports it. The accessors here therefore
leave those entries alone. \code{other_mort()} and \code{other_encounter()} list only
the contributions that do not belong to a component, and assigning through
them preserves the ones that do. This mirrors \code{\link[=other_params]{other_params()}}, which
likewise hides the parameters belonging to a component, and it makes it
impossible to wipe out a component's contribution by assigning a whole list.
}

\examples{
# An extra mortality that grows with the total biomass in the community.
# Like any custom rate function it has to live in the global environment
# or in a package, so that mizer can find it by name.
crowdingMort <- function(params, n, n_pp, n_other, t, component, ...) {
    biomass <- sum(n \%*\% (params@w * params@dw))
    # Same dimensions as the mortality rate: species x size
    0 * params@mu_b + 1e-14 * biomass
}
params <- NS_params
other_mort(params)[["crowding"]] <- "crowdingMort"
other_mort(params)

# The contribution is included in the mortality rate
range(getMort(params) - getMort(NS_params))

# and can be removed again
other_mort(params)[["crowding"]] <- NULL
other_mort(params)
}
\seealso{
\code{\link[=setComponent]{setComponent()}}, \code{\link[=other_params]{other_params()}}, \code{\link[=ext_mort]{ext_mort()}}, \code{\link[=ext_encounter]{ext_encounter()}}

Other extension tools:
\code{\link[=NOther]{NOther()}},
\code{\link[=coerceToExtensionClass]{coerceToExtensionClass()}},
\code{\link[=initialNOther<-]{initialNOther<-()}},
\code{\link[=recordExtension]{recordExtension()}},
\code{\link[=setComponent]{setComponent()}},
\code{\link[=setRateFunction]{setRateFunction()}}
}
\concept{extension tools}
