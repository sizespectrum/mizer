% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{resolve_cdf_power}
\alias{resolve_cdf_power}
\title{Resolve the power of weight for a cumulative distribution}
\usage{
resolve_cdf_power(power = NULL, biomass = NULL, per_log_size = NULL)
}
\arguments{
\item{power}{The power of weight multiplying the number density, or \code{NULL}.}

\item{biomass}{Whether to plot a biomass density rather than a number
density, or \code{NULL}. The default is \code{TRUE}.}

\item{per_log_size}{Whether to plot a density with respect to logarithmic
size rather than with respect to size, or \code{NULL}. The default is \code{FALSE}.}
}
\value{
A list with entries \code{power}, \code{biomass} and \code{per_log_size}.
}
\description{
As \code{\link[=resolve_spectrum_power]{resolve_spectrum_power()}}, except that \code{per_log_size = TRUE} is
rejected: integrating a density with respect to logarithmic size gives the
same cumulative quantity as integrating the corresponding density with
respect to size, so the flag would be meaningless here.
}
\keyword{internal}
