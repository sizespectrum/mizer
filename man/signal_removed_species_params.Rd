% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info_signals.R
\name{signal_removed_species_params}
\alias{signal_removed_species_params}
\title{Signal that species parameter columns have been removed}
\usage{
signal_removed_species_params(withdrawn)
}
\arguments{
\item{withdrawn}{A character vector of the withdrawn column names. Nothing is
signalled when it is empty.}
}
\value{
\code{NULL} invisibly. Called for its side effect of signalling.
}
\description{
A column that is absent from the table assigned to \code{\link[=species_params<-]{species_params<-()}} or
\code{\link[=given_species_params<-]{given_species_params<-()}} is one the user no longer supplies. Mizer
calculates afresh those it knows how to calculate, and the rest leave the
model altogether. This reports the ones that leave the given species
parameters, whether or not mizer puts a calculated value back.
}
\details{
The report is made at level 3, so it is visible in ordinary use and silent
from \code{info_level} 1 downwards: removing a column is what the user asked for,
not something that went differently from how they asked. It carries the
class \code{info_about_removed} for code that wants to catch it in particular.
}
\concept{info signalling functions}
