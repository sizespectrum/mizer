% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steadyState.R
\name{tuneSteadyState}
\alias{tuneSteadyState}
\title{Tune a model so that the state it is in becomes a steady state}
\usage{
tuneSteadyState(
  params,
  solver = c("project", "newton"),
  effort = params@initial_effort,
  preserve = c("reproduction_level", "erepro", "R_max"),
  info_level = default_info_level(),
  ...
)
}
\arguments{
\item{params}{A \linkS4class{MizerParams} object}

\item{solver}{The solver to use: \code{"project"} to run the dynamics until they
settle, \code{"newton"} to solve the steady-state equation directly. See
\emph{Choosing a solver}.}

\item{effort}{The fishing effort to use throughout. By default the initial
effort stored in \code{params}.}

\item{preserve}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Specifies whether the \code{reproduction_level} should be preserved (default)
or the maximum reproduction rate \code{R_max} or the reproductive
efficiency \code{erepro}. See \code{\link[=setBevertonHolt]{setBevertonHolt()}} for an explanation
of the \code{reproduction_level}.}

\item{info_level}{Controls the amount of information messages that are shown.
Higher levels lead to more messages, \code{info_level = 0} gives silence. The
default is taken from the \code{mizer_info_level} option, see
\code{\link[=default_info_level]{default_info_level()}}.}

\item{...}{Arguments for the chosen solver.

With \code{solver = "project"}: \code{t_max}, \code{t_per}, \code{dt}, \code{t_save}, \code{tol},
\code{amplitude_tol}, \code{amp_rel_tol}, \code{extinction_threshold}, \code{progress_bar} and
\code{method}, all as described in \code{\link[=projectUntilSettled]{projectUntilSettled()}}. Note that \code{tol}
here defaults to \code{0.1 * dt} and measures the largest relative change in
egg production, because the distance function is \code{\link[=distanceMaxRelRDI]{distanceMaxRelRDI()}}.

With \code{solver = "newton"}: \code{residual_tol} (default \code{1e-6}), a tolerance on
the per-capita rate of change passed to \code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}}; \code{maxit}
(default \code{200}); \code{jacobian}, either \code{"update"} (default, the Jacobian is
computed once and then updated cheaply each iteration — \code{nleqslv}'s
\code{"Broyden"}) or \code{"recompute"} (a numerical Jacobian at every iteration —
\code{nleqslv}'s \code{"Newton"}); \code{global}, the globalisation strategy (default
\code{"dbldog"}, a robust double-dogleg trust region); and \code{verbose} to trace
the iterations.}
}
\value{
A \code{MizerParams} object with the initial state replaced by the steady
state found, and with \code{erepro}/\code{R_max} and \code{cc_pp} adjusted as described
above. It carries a \code{"convergence"} attribute describing the solution
found; see \code{\link[=projectUntilSettled]{projectUntilSettled()}}. Check it: convergence is not
guaranteed.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Solves for the consumer size spectra while holding the reproduction rate
(RDD), the resource and any other components at the values stored in
\code{params}, and then adjusts the parameters that generate those held values so
that they are steady too. This is the function to use while setting up and
calibrating a model: holding the inputs to the fish dynamics fixed is what
makes the search reliable.
}
\details{
Concretely, three things are held fixed during the search and two parameters
are re-derived afterwards:
\itemize{
\item The reproduction rate is pinned at \code{getRDD(params)}. Afterwards, if the
model uses Beverton-Holt reproduction, \code{\link[=setBevertonHolt]{setBevertonHolt()}} restores the
density dependence so that it reproduces exactly that rate at the new
spectra. Use \code{preserve} to say which of the reproduction parameters should
be kept as it was.
\item The resource abundance is held at \code{initialNResource(params)}. Afterwards
the resource capacity \code{cc_pp} is recomputed so that this abundance is a
steady state of the resource under the new spectra. If the capacity had
been set by hand (frozen), it is rebalanced and thereby unfrozen.
\item Other components are held constant throughout and are not adjusted.
}

So the model you get back is at a fixed point of the \emph{full} dynamics, with
reproduction and the resource free, and \code{\link[=getStability]{getStability()}} can be applied to it
directly. Contrast \code{\link[=findSteadyState]{findSteadyState()}}, which changes no parameter and
instead lets the reproduction rate and the resource move to wherever the
parameters you already have put them.

Holding those inputs fixed is what makes the search reliable, but it does not
make the result certain: the state that is stored is only as close to a fixed
point as the solver's tolerance allowed, and with \code{solver = "project"} the
run may instead have stopped on a limit cycle or on a species going extinct.
Check the result rather than assuming it; see the section below.
}
\section{Choosing a solver}{
\code{solver = "project"} (the default) runs the dynamics until they settle, via
\code{\link[=projectUntilSettled]{projectUntilSettled()}}, using \code{\link[=distanceMaxRelRDI]{distanceMaxRelRDI()}} as its distance
function. It needs no extra packages and works with any resource dynamics.

\code{solver = "newton"} solves the steady-state equation directly with a
Newton-type root finder from the \code{nleqslv} package. It converges even when
the steady state is dynamically \strong{unstable}, where the time-stepping solver
cannot, and it discovers the support of the steady state automatically. It
starts from the spectra in \code{initialN(params)}, so a reasonable initial guess
still matters — for example the spectra from a nearby stable
parameterisation, or the (diverging) output of \code{solver = "project"}.

Because the resource is held fixed either way, \code{solver = "newton"} here does
not need the resource to be a semichemostat, unlike in \code{\link[=findSteadyState]{findSteadyState()}}
where the resource is one of the unknowns.
}

\section{What you get back may not be a steady state}{


The stopping criterion is a proxy. It says that two states \code{t_per} years
apart differ by less than \code{tol} on whatever scale the criterion is measured
on; it does not say that the state reached is a fixed point. There are four
ways the returned object can fail to be one:
\itemize{
\item the run converged on its own scale while the biomasses are still
visibly drifting, because \code{tol} was too loose (\code{type = "below_tolerance"}, which is why that type is not called \code{"steady"});
\item the run reached \code{t_max} without converging (\code{type = "not_converged"});
\item the run settled on a limit cycle (\code{type = "cycle"}), in which case the
state stored is one point on that cycle;
\item the run stopped because a species was going extinct
(\code{type = "extinction"}).
}

So treat the result as a claim to be checked rather than as a guarantee:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{attr(params, "convergence")$type      # "below_tolerance", "cycle", "not_converged", "extinction"
attr(params, "convergence")$residual  # largest biomass drift, in 1/year
isSteady(params)                      # TRUE if within tolerance
summary(params)                       # includes the biomass-drift verdict
plot(getSteadyResidual(params))       # which species, and at which sizes
}\if{html}{\out{</div>}}

The first four say \emph{whether} the model is steady, the last says \emph{where} it
is not, which is the one to reach for when it is not: a model that is off
steady state is usually off in one species or one part of the size range,
and the plot names it. See \code{\link[=getSteadyResidual]{getSteadyResidual()}} for why the verdict is
phrased in terms of biomass drift rather than the largest per-capita rate.

The messages this function prints say the same thing — a converged run
whose biomasses are still moving reports the drift and adds "Reduce \code{tol}
to converge further." — but they are suppressed by \code{info_level = 0}, so in
a script the \code{"convergence"} attribute is the reliable check.

Finally, a genuine fixed point need not be a \emph{stable} one. Use
\code{\link[=getStability]{getStability()}} to find out, and \code{solver = "newton"} to converge onto a
fixed point that the dynamics themselves would run away from.
}

\examples{
\donttest{
params <- newTraitParams()
species_params(params)$gamma[5] <- 3000
params <- tuneSteadyState(params)
plotSpectra(params)
}
}
\seealso{
\code{\link[=findSteadyState]{findSteadyState()}}, \code{\link[=projectUntilSettled]{projectUntilSettled()}},
\code{\link[=steadySingleSpecies]{steadySingleSpecies()}}, \code{\link[=isSteady]{isSteady()}}, \code{\link[=getSteadyResidual]{getSteadyResidual()}},
\code{\link[=getStability]{getStability()}}
}
