# Building documentation and the mizer website

Use this skill when updating package documentation, adding or renaming exported
functions, editing agent skills / cheatsheet vignettes, or generating website
artifacts.

## Auto-generated files — never edit directly

The following files are generated by build scripts and must not be edited by hand:
- `NAMESPACE` and `man/*.Rd` — generated from roxygen2 comments via `devtools::document()`
- `R/RcppExports.R` and `src/RcppExports.cpp` — generated via `devtools::document()` / `Rcpp::compileAttributes()`
- `vignettes/cheatsheet-*.Rmd` — generated from `inst/skills/*/SKILL.md` via `dev_scripts/build_cheatsheets.R`
- `vignettes/upgrading.Rmd` — generated from `inst/skills/upgrade-mizer-code/SKILL.md`
- `docs/llms.txt` and `inst/llms.txt` — generated via `dev_scripts/build_llms.R`

---

## 1. Cheatsheet vignettes and agent skills

Agent skills in `inst/skills/<topic>/SKILL.md` serve as the single source of truth for both AI agents and the pkgdown cheatsheet articles in `vignettes/cheatsheet-*.Rmd`.

### Adding or modifying a skill

1. Edit the markdown file at `inst/skills/<topic>/SKILL.md`.
2. Content intended strictly for AI agents (such as diagnostic decision trees or symptom tables) must be wrapped in `<!-- agent-only --> ... <!-- /agent-only -->`. The cheatsheet builder drops these blocks.
3. Regenerate all cheatsheet vignettes:
   ```r
   source("dev_scripts/build_cheatsheets.R")
   build_cheatsheets()
   ```
4. If modifying behaviour or deprecating code, update `inst/skills/upgrade-mizer-code/SKILL.md` (which regenerates `vignettes/upgrading.Rmd`).

---

## 2. Maintaining the AI documentation index (`llms.txt`)

`llms.txt` follows the [llms.txt standard](https://llmstxt.org/) to provide AI agents with a concise package summary and categorized function index.

It exists in two locations:
- `docs/llms.txt` — served online at <https://sizespectrum.org/mizer/llms.txt>.
- `inst/llms.txt` — installed with the package so `mizerAgents::setup_mizer_agent()` reads the index matching the installed mizer version.

### After running `pkgdown::build_site()`

`pkgdown::build_site()` automatically creates a raw `docs/llms.txt` prefixed with `README.md` (which contains badges and HTML). To restore the clean overview header and sync both locations:

```r
source("dev_scripts/build_llms.R")
build_llms()
```

- To change the overview text at the top of `llms.txt`, edit `pkgdown/llms-header.md`, then re-run `build_llms()`.
- Note: `llms-full.txt` has been removed. Function signatures are fetched dynamically from the installed package / R help system.

---

## 3. Pkgdown website configuration (`_pkgdown.yml`)

When adding, renaming, or removing exported functions or vignettes, update `_pkgdown.yml`:

- **Exported functions**: Add to the appropriate section under `reference:`.
- **Vignettes / Articles**: Add to the appropriate section under `articles:`.

### Fast iteration on specific pages

Rebuilding the entire website with `pkgdown::build_site()` is slow. Rebuild individual targets during development:

```r
# Rebuild a single article / vignette
pkgdown::build_article("model_description")

# Rebuild reference pages
pkgdown::build_reference()

# Rebuild the homepage (from index.md / README.md)
pkgdown::build_home()
```

---

## 4. Synchronising navbar to `mizerCourse`

If the top navigation bar configuration (`navbar:` in `_pkgdown.yml`) changes, sync it with the mizer course website:

```bash
Rscript dev_scripts/sync_course_navbar.R
# or specifying course path:
Rscript dev_scripts/sync_course_navbar.R --course ../mizerCourse
```

---

## 5. Generating developer wiki documentation

Developer skills in `.claude/skills/<topic>.md` serve as the single source of truth for AI agents and the GitHub wiki developer guides in `../mizer.wiki/`.

### Updating developer guides

1. Edit the developer skill in `.claude/skills/<topic>.md`.
2. Content intended strictly for AI agents must be wrapped in `<!-- agent-only --> ... <!-- /agent-only -->`. The wiki builder drops these blocks.
3. Regenerate the wiki pages:
   ```r
   source("dev_scripts/build_wiki.R")
   build_wiki()
   ```
   Or from bash:
   ```bash
   Rscript dev_scripts/build_wiki.R
   ```
   This automatically autolinks function names to their pkgdown reference pages and rewrites skill cross-references into wiki links.

---

## 6. Verification checklist before submitting PR

1. `devtools::document()` runs cleanly with no Rd warnings.
2. `source("dev_scripts/build_cheatsheets.R"); build_cheatsheets()` run if any `inst/skills/` file changed.
3. `source("dev_scripts/build_wiki.R"); build_wiki()` run if any `.claude/skills/` file changed.
4. `source("dev_scripts/build_llms.R"); build_llms()` run if `pkgdown::build_site()` was executed or `pkgdown/llms-header.md` changed.
5. `_pkgdown.yml` updated for new exports or vignettes.
6. `Collate:` updated in `DESCRIPTION` if any new file was added in `R/`.
